# Streamlit Cloud secrets 예시
# 실제 secrets.toml은 .gitignore로 제외됨.
# Streamlit Cloud에서는 앱 Settings → Secrets에 아래 내용을 그대로 붙여넣으세요.

# QR 페이로드 baseURL (배포 후 실제 URL로 교체)
BASE_URL = "https://yongindc-pyrosafe.streamlit.app"

# (선택) Supabase 연결 시
# [supabase]
# url = "https://xxx.supabase.co"
# anon_key = "eyJ..."
