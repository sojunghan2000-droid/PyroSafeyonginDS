# 260907 현장요구사항 반영 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 2026-09-04 현장 회의(박재나 프로, 삼성물산)에서 확정된 5개 항목을 반영한다 — ①가설컨테이너 등 건물 외 위치 등록 가능 ②시설관리·안전점검의 소프트 삭제 ③점검유형 카탈로그 5종→3종 통합(과거 데이터 일괄 이관) ④별지5 외 "점검결과 보고" 사진대지 신규 보고서 ⑤별지9(오동작 관리대장) 기능 제거.

**Architecture:** 기존 취소(cancelled)/숨김(archived) 소프트 삭제 패턴과 `rename_inspection_type` DB 함수를 재사용해 리스크를 낮추고, 신규 컬럼은 전부 `add column if not exists` 폴백 방식으로 추가한다. 사진 리포트는 `Deficiency`에 신규 사진 필드 2개(`photo_path` 조치 전/점검사진, `inspection_photo_path` 결과 무관 점검사진)를 추가해 `report_center.py`의 기존 ReportLab 파이프라인(`_photo_image`, `_styles`)을 재사용한다. 오동작 기능은 DB는 보존하고 3개 페이지의 UI 참조만 제거한다.

**Tech Stack:** Streamlit 1.30+, Supabase(supabase-py), ReportLab, Python 3.13.

**Spec:** `PRD.md` `## 260907 개발내용` 섹션 (5개 하위 항목 260907-1~5 + "영향받지 않는 범위").

## Global Constraints

- 이 repo는 pytest 스위트가 없다(`tests/`에는 `test_db_monitor.py`만 존재). 검증은 (a) `python -c "import ast; ast.parse(...)"` 구문 검사, (b) `python -c "import lib.data as data; ..."` 헤드리스 스모크(라이브 Supabase 조회), (c) Streamlit preview 순으로 진행한다.
- Supabase service_role 키는 클라이언트(브라우저)에서 사용 금지.
- 파일 인코딩 UTF-8. 한글 문자열/주석 유지.
- 신규 DB 컬럼은 전부 `add column if not exists ... default ...`로 안전 폴백. 데이터 계층(`lib/data.py`)은 컬럼 미마이그레이션 상태에서도 `.get(...)`으로 죽지 않아야 한다(기존 관례).
- 커밋 메시지 말미에 다음 두 줄을 반드시 포함:
  ```
  Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
  ```
- `git push`는 사용자가 명시적으로 요청할 때만 (자동 push 금지 — 기존 plan들의 "Task 마지막에 자동 push" 관례를 이번엔 따르지 않는다).
- 점검유형 카탈로그 이관(Task 8)의 `rename_inspection_type` 3회 호출은 **비가역**이다. 실행 전 스냅샷을 사용자에게 안내하고, 실제 실행은 사용자 승인 후 진행한다.

---

### Task 1: 가설컨테이너 신규 층("TEMP") 등록

**Files:**
- Create: `assets/floors/TEMP.png` (임시 배치도 — 실측 배치도 수령 전 placeholder)
- Modify: `pages_app/dashboard.py:15` (`LOCATION_FLOORS`)
- Modify: `lib/inspection_dialog.py:35` (`SPOT_FLOORS`)

**Interfaces:**
- Produces: 층 코드 `"TEMP"`가 두 목록에 존재 → 위치 마스터 신규 위치 등록, 시설관리 "신규 위치 만들기", 대시보드 층별 도면 미니맵에서 선택/노출 가능.

- [ ] **Step 1: placeholder 배치도 이미지 생성**

기존 8개 PNG(`assets/floors/1F.png` 등)와 동일한 위치에 임시 이미지를 만든다. 실제 사진/스케치가 아직 없으므로 회색 배경 + "가설동(TEMP) — 배치도 임시" 텍스트만 있는 최소 이미지로 시작한다.

```python
# scripts/make_temp_floor_placeholder.py (1회성 스크립트 — 실행 후 삭제해도 무방)
from PIL import Image, ImageDraw, ImageFont

img = Image.new("RGB", (1200, 900), color=(226, 232, 240))  # slate-200
draw = ImageDraw.Draw(img)
text = "가설동 (TEMP) — 배치도 임시\n실측 배치도 수령 시 이 파일을 교체하세요"
draw.multiline_text((60, 60), text, fill=(51, 65, 85), spacing=10)
draw.rectangle([40, 40, 1160, 860], outline=(148, 163, 184), width=3)
img.save("assets/floors/TEMP.png")
print("saved assets/floors/TEMP.png")
```

Run: `python scripts/make_temp_floor_placeholder.py`
Expected: `assets/floors/TEMP.png` 파일 생성, 출력 `saved assets/floors/TEMP.png`

- [ ] **Step 2: LOCATION_FLOORS에 추가**

`pages_app/dashboard.py:15`:

```python
LOCATION_FLOORS = ["PIT", "B2", "B1", "1F", "2F", "3F", "4F", "Roof", "TEMP"]
```

- [ ] **Step 3: SPOT_FLOORS에 추가**

`lib/inspection_dialog.py:35`:

```python
SPOT_FLOORS = ["PIT", "B2", "B1", "1F", "2F", "3F", "4F", "Roof", "TEMP"]
```

- [ ] **Step 4: 구문 검사 + 헤드리스 스모크**

```bash
python -c "import ast; ast.parse(open('pages_app/dashboard.py',encoding='utf-8').read()); ast.parse(open('lib/inspection_dialog.py',encoding='utf-8').read()); print('OK')"
python -c "from pages_app.dashboard import LOCATION_FLOORS; from lib.inspection_dialog import SPOT_FLOORS; assert 'TEMP' in LOCATION_FLOORS and 'TEMP' in SPOT_FLOORS; print('OK', LOCATION_FLOORS, SPOT_FLOORS)"
```
Expected: 두 번 모두 에러 없이 `OK` 출력

- [ ] **Step 5: Commit**

```bash
git add assets/floors/TEMP.png pages_app/dashboard.py lib/inspection_dialog.py scripts/make_temp_floor_placeholder.py
git commit -m "$(cat <<'EOF'
feat(location): 가설컨테이너용 신규 층 TEMP 추가 (placeholder 배치도)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

---

### Task 2: DB 마이그레이션 — 신규 컬럼 3종 (equipment.active, deficiencies.photo_path, deficiencies.inspection_photo_path)

**Files:**
- Create: `docs/superpowers/migrations/2026-09-07-260907-columns.sql`
- Modify: `supabase_schema.sql`

**Interfaces:**
- Produces: `equipment.active boolean default true`, `deficiencies.photo_path text`, `deficiencies.inspection_photo_path text`.

- [ ] **Step 1: 마이그레이션 SQL 작성**

Create `docs/superpowers/migrations/2026-09-07-260907-columns.sql`:

```sql
-- 260907 현장요구사항: 신규 컬럼 3종 (전부 추가적 변경, 기존 row는 기본값)
alter table equipment
  add column if not exists active boolean not null default true;

alter table deficiencies
  add column if not exists photo_path            text,  -- 조치 전(발견 시) 사진 — 기존 action_photo_path와 분리
  add column if not exists inspection_photo_path  text;  -- 결과 무관 점검사진 (양호 포함)
```

- [ ] **Step 2: supabase_schema.sql 동기화**

`supabase_schema.sql`의 `equipment` / `deficiencies` `create table` 블록에 위 3개 컬럼을 각각 추가(없으면 파일 하단에 `alter table` 문 append, 기존 관례).

- [ ] **Step 3: 사용자에게 마이그레이션 실행 안내**

이 SQL은 Supabase SQL 에디터에서 1회 실행 필요. 전부 `add column if not exists`라 실행 전에도 앱은 폴백으로 동작하지만, 실제 저장/조회는 실행 후 가능. **실행은 사용자 몫** — 명시적으로 전달할 것.

- [ ] **Step 4: Commit**

```bash
git add supabase_schema.sql docs/superpowers/migrations/2026-09-07-260907-columns.sql
git commit -m "$(cat <<'EOF'
chore(db): equipment.active · deficiencies.photo_path/inspection_photo_path 마이그레이션 SQL

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

---

### Task 3: 데이터 계층 — Equipment.active 필드 + retire/restore 함수

**Files:**
- Modify: `lib/data.py` (`Equipment` dataclass ~40-62, `_row_to_equipment` ~392-402, `add_equipment` ~661, 조회/쓰기 섹션)

**Interfaces:**
- Consumes: Task 2의 `equipment.active` 컬럼(없어도 `.get("active", True)` 폴백).
- Produces:
  - `Equipment.active: bool = True`
  - `retire_equipment(equipment_id: str) -> None`
  - `restore_equipment(equipment_id: str) -> None`
  - `load_equipment(include_retired: bool = False) -> list[Equipment]` (기본은 활성 장비만 반환하도록 시그니처 확장)

- [ ] **Step 1: dataclass에 필드 추가**

`lib/data.py`의 `Equipment` dataclass, `spot_id` 필드 아래에 추가:

```python
    spot_id: str | None = None
    # v1.9(260907): 소프트 삭제 — False면 목록에서 숨김(이력은 보존)
    active: bool = True
```

- [ ] **Step 2: _row_to_equipment에서 매핑**

```python
def _row_to_equipment(r: dict) -> Equipment:
    return Equipment(
        equipment_id=r["equipment_id"], location_id=r["location_id"],
        category=r["category"], equipment_name=r["equipment_name"],
        serial=r["serial"], qr_status=r["qr_status"],
        last_inspection=_d(r.get("last_inspection")),
        health_status=r["health_status"], floor=r["floor"], zone=r["zone"],
        pixel_x=r.get("pixel_x") or 0.0, pixel_y=r.get("pixel_y") or 0.0,
        inspection_types=list(r.get("inspection_types") or []),
        spot_id=r.get("spot_id"),
        active=bool(r.get("active", True)),
    )
```

- [ ] **Step 3: load_equipment에 include_retired 파라미터 추가**

기존 `load_equipment()` 함수(캐시된 `_equipment_rows()`를 감싸는 공개 함수 — 정확한 현재 시그니처는 파일에서 확인)를 다음과 같이 확장. 기본값은 활성 장비만 반환해 기존 호출부(전부 `data.load_equipment()`로 인자 없이 호출)의 동작을 바꾸지 않는다:

```python
def load_equipment(include_retired: bool = False) -> list[Equipment]:
    rows = _equipment_rows()
    eqs = [_row_to_equipment(r) for r in rows]
    if not include_retired:
        eqs = [e for e in eqs if e.active]
    return eqs
```

(기존 함수 본문이 이 형태와 다르면 — 예: 정렬/변환 로직이 더 있으면 — 그 로직은 유지하고 마지막에 `include_retired` 필터만 추가할 것.)

- [ ] **Step 4: retire_equipment / restore_equipment 작성 (add_equipment 근처)**

```python
def retire_equipment(equipment_id: str) -> None:
    """장비를 비활성화(소프트 삭제)한다. 이력은 보존."""
    _db().table("equipment").update({"active": False}).eq(
        "equipment_id", equipment_id
    ).execute()
    _equipment_rows.clear()


def restore_equipment(equipment_id: str) -> None:
    """비활성화된 장비를 복구한다."""
    _db().table("equipment").update({"active": True}).eq(
        "equipment_id", equipment_id
    ).execute()
    _equipment_rows.clear()
```

- [ ] **Step 5: 헤드리스 스모크**

```bash
python -c "import lib.data as data; eq=data.load_equipment(); eq_all=data.load_equipment(include_retired=True); print('active', len(eq), '/ total', len(eq_all))"
```
Expected: 에러 없이 `active N / total N` 출력 (마이그레이션 전이면 두 값 동일 — 전부 active=True 폴백)

- [ ] **Step 6: 구문 검사 + Commit**

```bash
python -c "import ast; ast.parse(open('lib/data.py',encoding='utf-8').read()); print('OK')"
git add lib/data.py
git commit -m "$(cat <<'EOF'
feat(data): 장비 소프트 삭제 — Equipment.active + retire/restore_equipment

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

---

### Task 4: 시설관리 UI — [삭제] 버튼 + "숨긴 장비 보기" 필터

**Files:**
- Modify: `pages_app/equipment_inventory.py` (필터 영역 ~531, 장비 목록 행 렌더, `_status_dialog`/속성 변경 모달 영역)

**Interfaces:**
- Consumes: `data.load_equipment(include_retired=...)`, `data.retire_equipment`, `data.restore_equipment`, `Equipment.active`.

- [ ] **Step 1: 필터 영역에 "숨긴 장비 보기" 토글 추가**

`pages_app/equipment_inventory.py`의 필터 컬럼(`f1, f2, cat_col, _ = st.columns([0.9, 0.9, 1.7, 1.9])` 부근, 실제 컬럼 구성은 파일에서 확인)에 체크박스를 추가:

```python
show_retired = st.checkbox("숨긴 장비 보기", value=False, key="eq_show_retired")
eq_all = data.load_equipment(include_retired=show_retired)
if not show_retired:
    eq_all = [e for e in eq_all if e.active]
```

(기존에 `data.load_equipment()`를 인자 없이 호출하던 지점을 위 `eq_all` 변수로 교체 — 인자 없는 호출은 Task 3에서 기본 활성만 반환하도록 이미 바뀌었으므로, "숨긴 장비 보기" 체크 시에만 `include_retired=True`로 재조회.)

- [ ] **Step 2: 행별 [삭제]/[복구] 버튼 추가**

기존 "변경" 버튼(`if st.button("변경", key=f"qr_btn_{e.equipment_id}", ...)`) 옆에 활성 상태에 따라 분기되는 버튼을 추가. 이미 회차/이력에 참조된 장비도 데이터는 지우지 않으므로 조건 없이 항상 노출 가능:

```python
if e.active:
    if st.button("삭제", key=f"eq_retire_{e.equipment_id}", use_container_width=True):
        data.retire_equipment(e.equipment_id)
        st.success(f"{e.equipment_id} 삭제(숨김) 처리되었습니다.")
        st.rerun()
else:
    st.caption("숨김됨")
    if st.button("복구", key=f"eq_restore_{e.equipment_id}", use_container_width=True):
        data.restore_equipment(e.equipment_id)
        st.success(f"{e.equipment_id} 복구되었습니다.")
        st.rerun()
```

행 렌더가 컬럼 리스트(`cols = st.columns([...])`) 기반이면 "속성" 컬럼 폭을 좁히고 신규 버튼 컬럼을 하나 추가하거나, 기존 "변경" 버튼 아래에 같은 컬럼 내 2번째 버튼으로 배치(정확한 컬럼 배치는 파일의 8컬럼 정의 — 장비 ID/시설종류/점검유형/위치등록/QR상태/최근점검/점검이력/속성 — 를 확인해 "속성" 컬럼에 세로로 두 버튼을 쌓는 방식을 권장).

- [ ] **Step 3: 구문 검사 + Commit**

```bash
python -c "import ast; ast.parse(open('pages_app/equipment_inventory.py',encoding='utf-8').read()); print('OK')"
git add pages_app/equipment_inventory.py
git commit -m "$(cat <<'EOF'
feat(equipment): 장비 [삭제]/[복구] 버튼 + 숨긴 장비 보기 필터

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

---

### Task 5: 데이터 계층 — 회차 원클릭 삭제 (delete_round)

**Files:**
- Modify: `lib/data.py` (`cancel_round`/`archive_round` 근처)

**Interfaces:**
- Consumes: 기존 `cancel_round(round_id, reason, by) -> bool`, `archive_round(round_id) -> bool`(정확한 시그니처는 파일에서 확인 — 없으면 아래 Step 1에서 동일 패턴으로 작성).
- Produces: `delete_round(round_id: str, by: str) -> bool` — 완료 Task가 0건인 회차만 성공.

- [ ] **Step 1: 완료 Task 0건 여부 확인 헬퍼 확인/작성**

`tasks_of_round(round_id, include_excluded=True)` 기존 함수(별지5/6 코드에서 이미 사용 확인됨)를 재사용해 완료 Task 유무를 판단:

```python
def _round_has_completed_task(round_id: str) -> bool:
    return any(
        t.status == "Completed"
        for t in tasks_of_round(round_id, include_excluded=True)
    )
```

- [ ] **Step 2: delete_round 작성**

```python
def delete_round(round_id: str, by: str) -> bool:
    """완료된 Task가 하나도 없는 회차를 취소+숨김 한 번에 처리(원클릭 삭제).
    완료 Task가 있으면 거부(False) — 그 경우는 기존 point-in-time 취소(사유 입력)만 허용."""
    r = get_round(round_id)
    if not r or r.cancelled or _round_has_completed_task(round_id):
        return False
    if not cancel_round(round_id, "생성 취소", by):
        return False
    return archive_round(round_id)
```

(`archive_round`의 실제 함수명이 다르면 — 기존 취소 회차 plan(Task "취소 회차 숨김/복구")에서 도입된 실제 이름을 파일에서 확인해 맞출 것. 반환형이 `bool`이 아니면 그에 맞춰 성공 판정 로직 조정.)

- [ ] **Step 3: 헤드리스 스모크**

```bash
python -c "import lib.data as data; print(hasattr(data, 'delete_round'))"
```
Expected: `True`

- [ ] **Step 4: 구문 검사 + Commit**

```bash
python -c "import ast; ast.parse(open('lib/data.py',encoding='utf-8').read()); print('OK')"
git add lib/data.py
git commit -m "$(cat <<'EOF'
feat(data): 완료 Task 0건 회차 원클릭 삭제 — delete_round

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

---

### Task 6: 안전점검 UI — 회차 상세 [삭제] 버튼

**Files:**
- Modify: `pages_app/inspection_tasks.py` (회차 상세 모달, 기존 "점검 취소" 버튼 로직 부근)

**Interfaces:**
- Consumes: `data.delete_round(round_id, by) -> bool`, `data.tasks_of_round`.

- [ ] **Step 1: 완료 Task 0건이면 [삭제] 버튼, 있으면 기존 [점검 취소] 유지**

회차 상세 모달의 "점검 취소" 버튼이 있는 영역(기존 `can_cancel = (not r.cancelled) and r.status != "Completed"` 근처)에 분기 추가:

```python
_round_tasks_all = data.tasks_of_round(round_id, include_excluded=True)
_has_completed = any(t.status == "Completed" for t in _round_tasks_all)

if not r.cancelled and not _has_completed:
    if st.button("삭제", key=f"round_delete_{round_id}", use_container_width=True,
                 help="완료된 점검이 없는 회차를 즉시 삭제(숨김)합니다."):
        by = (auth.current_user() or {}).get("name") or "관리자"
        if data.delete_round(round_id, by):
            st.success(f"{round_id} 삭제되었습니다.")
            st.session_state["_inspection_tasks_close_dialog"] = True
            st.rerun()
        else:
            st.error("삭제할 수 없는 회차입니다.")
# else: 기존 [점검 취소] 버튼 로직 그대로 유지 (Task가 하나라도 진행된 회차)
```

(모달을 닫는 정확한 방법은 파일의 다른 "저장 후 닫기" 패턴을 따를 것 — `st.rerun()`만으로 모달이 자동으로 닫히는 기존 관례면 `_inspection_tasks_close_dialog` 세션 상태 줄은 생략 가능. 기존 점검 취소 버튼과 나란히 배치하되 상호 배타적으로 노출.)

- [ ] **Step 2: 구문 검사 + Commit**

```bash
python -c "import ast; ast.parse(open('pages_app/inspection_tasks.py',encoding='utf-8').read()); print('OK')"
git add pages_app/inspection_tasks.py
git commit -m "$(cat <<'EOF'
feat(tasks): 완료 Task 없는 회차 [삭제] 버튼 (원클릭 취소+숨김)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

---

### Task 7: 점검유형 카탈로그 재편 — 5종 → 3종 코드 변경 (데이터 이관은 별도 승인 후 실행)

**Files:**
- Modify: `lib/data.py` (`TASK_INSPECTION_TYPES` ~80-86, `INSPECTION_TYPE_CATEGORY_DEFAULTS` ~89-104)
- Create: `docs/superpowers/migrations/2026-09-07-inspection-type-consolidation.sql`

**Interfaces:**
- Consumes: 기존 `rename_inspection_type` Postgres 함수(2026-07-09 도입, 카탈로그·회차·Task·장비 참조 한 트랜잭션 갱신).
- Produces: 카탈로그 3종 — `["일일 점검", "월간 점검", "특별 점검"]`.

- [ ] **Step 1: TASK_INSPECTION_TYPES 3종으로 축소**

```python
TASK_INSPECTION_TYPES = [
    "일일 점검",   # 화기작업구간 점검용 — 작업 시작 전/중 수시
    "월간 점검",
    "특별 점검",   # v1.9(260907): 분기 점검 + 연간 점검 통합
]
```

- [ ] **Step 2: INSPECTION_TYPE_CATEGORY_DEFAULTS에서 분기/연간 → 특별 점검으로 치환**

```python
INSPECTION_TYPE_CATEGORY_DEFAULTS: dict[str, list[str]] = {
    "소화기": ["월간 점검", "특별 점검"],
    "확산소화기": ["월간 점검", "특별 점검"],
    "간이소화장치": ["월간 점검"],
    "비상경보장치": ["월간 점검"],
    "가스누설경보기": ["월간 점검"],
    "간이피난유도선": ["월간 점검"],
    "방화포": ["월간 점검"],
    "감지기": ["특별 점검"],
    "발신기": ["특별 점검"],
    "수신기": ["특별 점검"],
    "유도등": ["월간 점검"],
    "스프링클러": ["특별 점검"],
    "소화전": ["특별 점검"],
    "기타": [],
}
```

- [ ] **Step 3: 데이터 이관 SQL 작성 (실행은 사용자 승인 후 별도)**

Create `docs/superpowers/migrations/2026-09-07-inspection-type-consolidation.sql`:

```sql
-- 260907 현장요구사항: 점검유형 카탈로그 5종 → 3종 통합
-- 비가역 — 실행 전 inspection_types / inspection_rounds / inspection_tasks 백업 권장.
-- rename_inspection_type(old, new)는 카탈로그·회차·Task·장비 참조를 한 트랜잭션으로 갱신한다.
select rename_inspection_type('주간 점검', '일일 점검');
select rename_inspection_type('분기 점검', '특별 점검');
select rename_inspection_type('연간 점검', '특별 점검');
```

- [ ] **Step 4: 이관 실행 안내 (사용자 승인 필수)**

이 SQL은 되돌릴 수 없다. Supabase SQL 에디터에서 **사용자가 직접, 명시적 승인 후** 1회 실행. 실행 전 관련 3개 테이블 백업(Supabase 대시보드에서 CSV export 또는 스냅샷)을 권장 문구로 안내할 것. 이 작업 자체는 이 Task의 코드 커밋과 분리해 사용자에게 별도로 요청한다.

- [ ] **Step 5: 구문 검사 + 헤드리스 스모크**

```bash
python -c "import ast; ast.parse(open('lib/data.py',encoding='utf-8').read()); print('OK')"
python -c "import lib.data as data; print(data.TASK_INSPECTION_TYPES); print(data.load_inspection_types())"
```
Expected: `TASK_INSPECTION_TYPES` = `['일일 점검', '월간 점검', '특별 점검']`. `load_inspection_types()`는 DB `inspection_types` 테이블이 아직 5종 그대로면 5종을 반환할 수 있음(하드코딩 폴백이 아니라 실제 DB 우선 조회이므로) — 이 경우 이관 SQL 실행 전까지는 관리자 메뉴 [점검 유형]에서 수동으로 "주간 점검" 비활성화 + "특별 점검" 추가로 임시 대응 가능함을 사용자에게 안내.

- [ ] **Step 6: Commit**

```bash
git add lib/data.py docs/superpowers/migrations/2026-09-07-inspection-type-consolidation.sql
git commit -m "$(cat <<'EOF'
feat(data): 점검유형 카탈로그 코드 3종으로 축소 + 데이터 이관 SQL 작성

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

---

### Task 8: Deficiency 사진 필드 확장 — 조치 전 사진 분리 + 결과 무관 점검사진

**Files:**
- Modify: `lib/data.py` (`Deficiency` dataclass ~256-287, `_row_to_deficiency` ~443-459, 쓰기 payload dict ~1004-1005)
- Modify: `lib/inspection_dialog.py` (`task_inspect_inline` ~1524-1533 사진 업로드, ~1636-1638 photo 저장, ~1657-1678 `add_deficiency` 호출)

**Interfaces:**
- Produces: `Deficiency.photo_path: str | None`(조치 전/발견 시 사진), `Deficiency.inspection_photo_path: str | None`(결과 무관 점검사진). 기존 `Deficiency.action_photo_path`(조치 후 사진, `record_deficiency_action`이 채움)는 의미·동작 변경 없음.

**주의 (기존 동작 버그 수정 포함):** 현재 `task_inspect_inline`은 불량 시 업로드받는 "조치 사진"을 `action_photo_path`에 저장한다(1673행). 이건 원래 "발견 시 사진"인데 필드명 때문에 `record_deficiency_action`(조치 완료 시 별도 사진 업로드)이 나중에 같은 필드를 덮어쓸 수 있다. 이번에 `photo_path`(발견 시) 필드를 신설해 분리하고, `action_photo_path`는 `record_deficiency_action`(조치 후)만 쓰도록 정리한다. 별지6은 계속 `action_photo_path`를 쓰므로(조치 결과 사진이 목적) 별지6 동작은 변하지 않는다.

- [ ] **Step 1: dataclass 필드 추가**

`lib/data.py`의 `Deficiency`, `checklist_items` 필드 위/아래에 추가:

```python
    action_photo_path: str | None = None
    submitter: str | None = None
    # v1.9(260907): 조치 전(발견 시) 사진 — action_photo_path(조치 후)와 분리
    photo_path: str | None = None
    # v1.9(260907): 점검 결과(양호/불량) 무관 점검사진
    inspection_photo_path: str | None = None
```

`__post_init__`은 변경 불필요(둘 다 `None` 기본값, 리스트 아님).

- [ ] **Step 2: _row_to_deficiency 매핑 추가**

```python
def _row_to_deficiency(r: dict) -> Deficiency:
    return Deficiency(
        deficiency_id=r["deficiency_id"], inspection_date=_d(r["inspection_date"]),
        inspector=r["inspector"], floor=r["floor"], zone=r["zone"],
        inspection_types=list(r.get("inspection_types") or []),
        issue=r["issue"], resolution=r["resolution"],
        confirmer=r.get("confirmer"), notice_no=r.get("notice_no"),
        task_id=r.get("task_id"),
        action_done=bool(r.get("action_done") or False),
        action_at=_d(r.get("action_at")),
        action_note=r.get("action_note") or "",
        action_photo_path=r.get("action_photo_path"),
        submitter=r.get("submitter"),
        defect_codes=list(r.get("defect_codes") or []),
        defect_other=r.get("defect_other") or "",
        checklist_items=dict(r.get("checklist_items") or {}),
        photo_path=r.get("photo_path"),
        inspection_photo_path=r.get("inspection_photo_path"),
    )
```

- [ ] **Step 3: record_deficiency_action의 payload dict에 두 필드 반영 (있으면)**

`lib/data.py` ~1004-1005 부근(딕셔너리로 Deficiency를 직렬화하는 지점 — 존재 시)에 `"photo_path": d.photo_path`, `"inspection_photo_path": d.inspection_photo_path`를 추가. 이 dict가 `add_deficiency`의 insert payload라면 여기서 두 필드가 함께 저장되도록 확인.

- [ ] **Step 4: task_inspect_inline — 결과 무관 점검사진 업로드 UI 추가**

`lib/inspection_dialog.py`의 "점검 결과" radio(`result = st.radio(...)`, ~1429) **바로 위**에 결과와 무관하게 항상 보이는 선택적 업로드를 추가:

```python
    insp_photo = photo_input(
        "점검사진 (선택)",
        key=f"tsk_insp_photo_{task_id}",
        help_text="점검 현장 사진(결과 무관, 1장). 모바일은 카메라 촬영 탭 이용.",
    )
```

- [ ] **Step 5: task_inspect_inline — 발견 시 사진을 photo_path로 분리 저장**

기존 ~1629-1638 블록:

```python
        # 사진 업로드 — v1.6: 불량 시 항상, 양호 시 없음
        photo_bytes = (
            action_photo_now.getvalue()
            if (action_photo_now and result == "불량")
            else None
        )
        new_def_id = data.next_deficiency_id()
        photo_path = None
        if photo_bytes:
            photo_path = data._upload_action_photo(new_def_id, photo_bytes)
```

이 블록 아래에 점검사진 업로드 처리 추가:

```python
        insp_photo_bytes = insp_photo.getvalue() if insp_photo else None
        insp_photo_path = None
        if insp_photo_bytes:
            insp_photo_path = data._upload_action_photo(
                f"{new_def_id}-insp", insp_photo_bytes
            )
```

- [ ] **Step 6: add_deficiency 호출 — action_photo_path → photo_path로 필드 교체 + inspection_photo_path 추가**

~1657-1678 `data.add_deficiency(data.Deficiency(...))` 호출에서:

```python
            # v1.9(260907): 발견 시 사진은 photo_path로 이동. 단, "현장에서 즉시 조치 완료"를
            # 체크한 경우는 같은 사진이 조치 결과 사진이기도 하므로 action_photo_path에도 그대로
            # 채워야 별지6(조치 결과 사진 컬럼)이 계속 사진을 보여준다. 즉시조치가 아니면 None으로
            # 시작해, 나중에 [작업 조치 관리] record_deficiency_action이 조치 후 사진으로 채운다.
            action_photo_path=(photo_path if action_immediate else None),
            submitter=inspector,
            defect_codes=defect_codes_selected,
            defect_other=defect_other_text.strip(),
            checklist_items=checklist_items,
            photo_path=photo_path,                    # 발견 시(조치 전) 사진
            inspection_photo_path=insp_photo_path,     # 결과 무관 점검사진
```

(기존 `action_photo_path=photo_path,` 줄을 위와 같이 교체. **주의(사전 검토에서 발견된 결함 수정)**: 단순히 `action_photo_path=None`으로 바꾸면 "현장 즉시 조치 완료" 체크 건은 조치 완료 상태(`action_done=True`)로 생성되어 이후 `record_deficiency_action`이 호출될 일이 없으므로 `action_photo_path`가 영원히 비어 별지6 사진 컬럼이 깨진다 — 위 3항 조건식으로 이 회귀를 막는다.)

- [ ] **Step 7: 구문 검사 + 헤드리스 스모크**

```bash
python -c "import ast; ast.parse(open('lib/data.py',encoding='utf-8').read()); ast.parse(open('lib/inspection_dialog.py',encoding='utf-8').read()); print('OK')"
python -c "import lib.data as data; d=data.Deficiency(deficiency_id='X',inspection_date=data.TODAY,inspector='t',floor='B1',zone='A',inspection_types=[],issue='양호',resolution='완료',confirmer=None,notice_no=None); print(d.photo_path, d.inspection_photo_path, d.action_photo_path)"
```
Expected: 마지막 출력 `None None None` (신규 필드 기본값 확인)

- [ ] **Step 8: Commit**

```bash
git add lib/data.py lib/inspection_dialog.py
git commit -m "$(cat <<'EOF'
feat(data): Deficiency 사진 필드 확장 — photo_path(조치 전)/inspection_photo_path(점검사진) 분리

action_photo_path가 발견 시 사진과 조치 후 사진을 겸용하던 걸 분리해
점검결과 보고(사진대지) 신규 리포트가 전/후 사진 쌍을 정확히 담을 수 있게 함.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

---

### Task 9: 보고서 센터 — "점검결과 보고(사진대지)" 신규 PDF

**Files:**
- Modify: `pages_app/report_center.py` (별지9 섹션을 대체하는 위치, `_build_pdf_byeolji9` 근처 함수 추가, `render()` 내 섹션 배치)

**Interfaces:**
- Consumes: `data.load_deficiencies()`, `data.load_tasks()`, `data.load_rounds()`, `data._db()`, `data.ACTION_PHOTO_BUCKET`, `_photo_image`, `_styles` (기존 helper 재사용).
- Produces: `_build_pdf_inspection_photo_report(round_id: str | None = None) -> bytes`.

- [ ] **Step 1: 사진 다운로드 공용 헬퍼 추가 (photo_path 필드명 파라미터화)**

기존 `_byeolji6_get_photo`(action_photo_path 전용)를 일반화한 헬퍼를 새로 추가(기존 함수는 건드리지 않음):

```python
def _download_photo(path: str | None) -> bytes | None:
    """임의의 Storage 경로에서 사진 bytes를 가져옴 (photo_path/action_photo_path/inspection_photo_path 공용)."""
    if not path:
        return None
    try:
        return data._db().storage.from_(data.ACTION_PHOTO_BUCKET).download(path)
    except Exception:
        return None
```

- [ ] **Step 2: PDF 생성 함수 작성**

`_build_pdf_byeolji9` 함수 앞뒤 어디든(별지9 섹션 자체는 Task 10에서 제거되므로 근처 위치가 자연스러움) 추가:

```python
# ---------- 점검결과 보고 (사진대지) — 260907 신규 ----------

def _build_pdf_inspection_photo_report(round_id: str | None = None) -> bytes:
    """별지5 외 범용 사진대지 보고서.
    섹션 A: 점검구간별 점검사진(inspection_photo_path, 결과 무관).
    섹션 B: 지적사항 조치 전/후 사진(photo_path / action_photo_path)."""
    from reportlab.lib import colors
    from reportlab.lib.pagesizes import A4
    from reportlab.lib.units import mm
    from reportlab.platypus import Paragraph, SimpleDocTemplate, Table, TableStyle, PageBreak

    s = _styles()
    buf = BytesIO()
    doc = SimpleDocTemplate(
        buf, pagesize=A4,
        leftMargin=15 * mm, rightMargin=15 * mm,
        topMargin=15 * mm, bottomMargin=15 * mm,
    )

    deficiencies = data.load_deficiencies()
    if round_id:
        round_tasks = {t.task_id for t in data.tasks_of_round(round_id, include_excluded=True)}
        deficiencies = [d for d in deficiencies if d.task_id in round_tasks]

    flowables = []

    # ---- 섹션 A: 점검결과 보고 (점검사진, 결과 무관) ----
    with_insp_photo = [d for d in deficiencies if d.inspection_photo_path]
    flowables.append(Paragraph("점검결과 보고 — 점검사진", s["title"]))
    if not with_insp_photo:
        flowables.append(Paragraph("등록된 점검사진이 없습니다.", s["left"]))
    else:
        COL_W_A = [30 * mm, 75 * mm, 75 * mm]  # 180mm 합
        rows = [[Paragraph("점검구간", s["h"]), Paragraph("점검사진", s["h"]), Paragraph("비고", s["h"])]]
        row_heights = [8 * mm]
        for d in with_insp_photo:
            photo = _photo_image(_download_photo(d.inspection_photo_path), max_w_mm=70, max_h_mm=55)
            rows.append([
                Paragraph(f"{d.floor}<br/>{d.zone}", s["cell"]),
                photo if photo else Paragraph("사진 로드 실패", s["cell"]),
                Paragraph(d.issue or "", s["left"]),
            ])
            row_heights.append(60 * mm)
        tbl = Table(rows, colWidths=COL_W_A, rowHeights=row_heights, repeatRows=1)
        tbl.setStyle(TableStyle([
            ("GRID", (0, 0), (-1, -1), 0.5, colors.black),
            ("VALIGN", (0, 0), (-1, -1), "MIDDLE"),
            ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#F1F5F9")),
        ]))
        flowables.append(tbl)

    flowables.append(PageBreak())

    # ---- 섹션 B: 지적사항 조치 진/후 사진대지 ----
    with_before_after = [d for d in deficiencies if d.photo_path and d.action_photo_path]
    flowables.append(Paragraph("지적사항 조치 진/후 사진대지", s["title"]))
    if not with_before_after:
        flowables.append(Paragraph("조치 전/후 사진이 모두 등록된 지적사항이 없습니다.", s["left"]))
    else:
        COL_W_B = [30 * mm, 75 * mm, 75 * mm]
        rows = [[Paragraph("점검구간", s["h"]), Paragraph("조치 前", s["h"]), Paragraph("조치 後", s["h"])]]
        row_heights = [8 * mm]
        for d in with_before_after:
            before = _photo_image(_download_photo(d.photo_path), max_w_mm=70, max_h_mm=55)
            after = _photo_image(_download_photo(d.action_photo_path), max_w_mm=70, max_h_mm=55)
            rows.append([
                Paragraph(f"{d.floor}<br/>{d.zone}", s["cell"]),
                before if before else Paragraph("사진 로드 실패", s["cell"]),
                after if after else Paragraph("사진 로드 실패", s["cell"]),
            ])
            row_heights.append(60 * mm)
        tbl = Table(rows, colWidths=COL_W_B, rowHeights=row_heights, repeatRows=1)
        tbl.setStyle(TableStyle([
            ("GRID", (0, 0), (-1, -1), 0.5, colors.black),
            ("VALIGN", (0, 0), (-1, -1), "MIDDLE"),
            ("BACKGROUND", (0, 0), (-1, 0), colors.HexColor("#F1F5F9")),
        ]))
        flowables.append(tbl)

    doc.build(flowables)
    return buf.getvalue()
```

- [ ] **Step 3: render()에 별지5/6과 동일한 회차 드롭다운 섹션 추가**

`render()` 함수의 별지6 섹션(`_spacer()` 다음)과 별지9 섹션 사이에 삽입:

```python
    # ---------- 점검결과 보고 (사진대지, 260907 신규) ----------
    _section_title("점검결과 보고 · 사진대지",
                   "별지5 법정 서식 외 범용 사진 증빙 보고서. "
                   "점검사진 + 지적사항 조치 전/후 사진을 회차 단위로 출력합니다.")
    _, midp, _ = st.columns([1, 2, 1])
    with midp:
        st.markdown(_card_header("점검결과 보고", "사진대지"), unsafe_allow_html=True)
        _optsp = {"전체 (모든 회차)": None}
        for _r in sorted(data.load_rounds(), key=lambda x: x.due_date, reverse=True):
            if getattr(_r, "cancelled", False) or _r.task_type == data.MAL_ROUND_TYPE:
                continue
            _optsp[f"{_r.round_id} · {_r.task_type}"] = _r.round_id
        _sel_labelp = st.selectbox("출력 범위", list(_optsp.keys()), key="insp_photo_report_scope")
        _sel_roundp = _optsp[_sel_labelp]
        _fnamep = (f"점검결과 보고 - {_sel_roundp}.pdf" if _sel_roundp else "점검결과 보고.pdf")
        st.download_button(
            "Download 점검결과 보고 PDF",
            data=_build_pdf_inspection_photo_report(_sel_roundp),
            file_name=_fnamep,
            mime="application/pdf",
            use_container_width=True,
            type="primary",
            key="insp_photo_report_dl",
        )
    _spacer()
```

- [ ] **Step 4: 구문 검사 + 헤드리스 스모크**

```bash
python -c "import ast; ast.parse(open('pages_app/report_center.py',encoding='utf-8').read()); print('OK')"
python -c "
import pages_app.report_center as rc
pdf = rc._build_pdf_inspection_photo_report(None)
print('pdf bytes', len(pdf))
"
```
Expected: `pdf bytes N` (N > 0, 빈 데이터라도 "없습니다" 문구가 담긴 유효 PDF)

- [ ] **Step 5: Commit**

```bash
git add pages_app/report_center.py
git commit -m "$(cat <<'EOF'
feat(report): 점검결과 보고(사진대지) 신규 PDF — 점검사진 + 조치 전/후 회차별 출력

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

---

### Task 10: 별지9(오동작 관리대장) 기능 제거 — UI만, DB는 보존

**Files:**
- Modify: `pages_app/inspection_tasks.py` (오동작 버튼 ~452-474/502-507, 결과 3분기 관련 회차 상세 표시 ~254-289, 감사로그 등은 유지)
- Modify: `lib/inspection_dialog.py` (`task_inspect_inline`의 결과 radio 3분기 → 2분기, 오동작 입력 블록 제거)
- Modify: `pages_app/deficiency_manager.py` (오동작 통합 리스트/필터/KPI/조치 모달 트리거 제거)
- Modify: `pages_app/report_center.py` (별지9 섹션 제거)

**Interfaces:**
- Consumes 없음. `data.load_malfunctions`/`data.add_malfunction`/`data.record_malfunction_action` 등 데이터 계층 함수는 **삭제하지 않음**(테이블 보존, 코드 참조만 제거).

- [ ] **Step 1: inspection_tasks.py — [오동작 등록] 버튼 제거**

`pages_app/inspection_tasks.py` ~452-474:
- 상단 버튼 `st.columns(3)` → `st.columns(2)`로 축소, `b2`(오동작 등록)를 제거하고 감사 로그 내보내기를 `b2`로 이동
- CSS 셀렉터 문자열에서 `.st-key-open_new_malfunction_insp` 부분 제거
- `mal_clicked = False` 변수 및 관련 `if mal_clicked:` 블록(~503-507) 전체 삭제

```python
        b1, b2 = st.columns(2)
        with b1:
            if st.button("신규 일정 등록", type="primary",
                         use_container_width=True, key="open_new_task"):
                new_task_clicked = True
        with b2:
            st.download_button(
                "감사 로그 내보내기",
                data=_audit_log_xlsx(),
                file_name=f"감사로그_점검결과_{data.TODAY:%Y%m%d}.xlsx",
                mime="application/vnd.openxmlformats-officedocument."
                     "spreadsheetml.sheet",
                use_container_width=True,
                key="audit_log_export",
                help="점검 결과(별지5 원본) 전체를 Excel(.xlsx)로 내려받습니다.",
            )
```

- [ ] **Step 2: inspection_tasks.py — 회차 Task 목록의 오동작 배지 블록 제거**

~254-289 "오동작 우선 확인 (Malfunction)" 블록 전체 삭제. `mal_match` 관련 코드 제거하고 바로 `elif t.status == "Completed":`(현재 ~290행)부터 시작하도록 `if`→`if`로 정리(분기 재구성).

- [ ] **Step 3: inspection_dialog.py — 결과 radio 2분기로 축소 + 오동작 입력 블록 제거**

`task_inspect_inline`(~1429-1487):

```python
    result = st.radio(
        "결과", ["양호", "불량"], horizontal=True,
        label_visibility="collapsed", key=f"tsk_res_{task_id}",
    )
```

이어지는 "오동작 입력 영역 (v1.5+)" 블록(`if result == "오동작":` ~1444-1487) 전체 삭제. `mal_category`/`mal_detail`/`mal_occurred` 변수 선언(~1441-1443)도 함께 제거.

- [ ] **Step 4: inspection_dialog.py — 제출 버튼의 오동작 분기 제거**

`if st.button("점검 결과 제출", ...)` 블록 내 `if result == "오동작": ... return`(~1563-1591) 전체 삭제.

- [ ] **Step 5: inspection_dialog.py — malfunction_dialog/malfunction_action_dialog 함수 자체는 남기되 호출부만 제거**

함수 정의(`malfunction_dialog`, `malfunction_action_dialog`, ~1702, ~1797)는 그대로 둔다(데이터 보존 원칙 — 필요 시 향후 복원 가능). 호출부만 Step 1에서 이미 제거됨.

- [ ] **Step 6: deficiency_manager.py — 오동작 관련 UI 제거**

`pages_app/deficiency_manager.py`에서:
- 별지9 오동작을 통합 row로 변환하는 루프(~74-86 "별지9 오동작 (v1.5+)") 제거
- KPI 카드에서 오동작 카운트(`cnt_mal`, ~223, ~229, ~233) 제거
- 필터 옵션에서 `"오동작"` 제거(~245 `["전체", "지적사항", "오동작", "조치 대기만"]` → `["전체", "지적사항", "조치 대기만"]`)
- 조치 입력 분기(~338-341 `elif r.status == "조치 대기" and r.type == "오동작":`) 제거
- 오동작 조치 입력 모달 트리거(~210-218 `_open_malfunction_action` 관련) 제거
- import 목록(~11)에서 `malfunction_dialog`(사용처 있으면 유지, 없으면 제거)는 실제 남은 참조를 확인해 정리

- [ ] **Step 7: report_center.py — 별지9 섹션 제거**

`render()` 함수 내 "---------- 별지9 ----------" 섹션(~607-621) 전체 삭제. `_build_pdf_byeolji9` 함수 정의(~367 이하)는 남겨도 무방(미호출 상태로 방치 — 데이터 계층처럼 보존).

- [ ] **Step 8: 구문 검사 + 헤드리스 스모크**

```bash
python -c "
import ast
for f in ['pages_app/inspection_tasks.py','lib/inspection_dialog.py','pages_app/deficiency_manager.py','pages_app/report_center.py']:
    ast.parse(open(f, encoding='utf-8').read())
print('OK')
"
python -c "import lib.data as data; print('malfunctions still loadable:', len(data.load_malfunctions()))"
```
Expected: `OK`, 그리고 오동작 데이터는 여전히 조회 가능(삭제되지 않았음 확인)

- [ ] **Step 9: Commit**

```bash
git add pages_app/inspection_tasks.py lib/inspection_dialog.py pages_app/deficiency_manager.py pages_app/report_center.py
git commit -m "$(cat <<'EOF'
feat: 별지9(오동작 관리대장) UI 제거 — 전기팀 수기 관리로 전환, DB는 보존

안전점검 결과 3분기(양호/불량/오동작) → 2분기로 축소.
오동작 등록 진입점·통합 리스트·조치 모달·별지9 PDF 섹션 제거.
데이터 계층 함수(load_malfunctions 등)와 malfunctions 테이블은 유지 — 과거 이력 보존.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

---

### Task 11: Streamlit preview 검증 + PRD 최종 동기화

**Files:**
- Modify: `PRD.md` (6.1/6.2/6.3/6.6 관련 서술에 260907 변경 반영, 로드맵에 v1.9 추가)

- [ ] **Step 1: preview 기동 및 수동 검증**

로컬 Streamlit 서버 기동 후(`streamlit run app.py` 또는 기존 preview 스크립트) 다음을 확인:
1. 시설관리 → "신규 위치 만들기"에서 층 `TEMP` 선택 가능, 저장 후 도면 미리보기에 배경 이미지(placeholder) 노출
2. 시설관리 장비 행 [삭제] → 목록에서 사라짐 → "숨긴 장비 보기" 체크 → 다시 노출 → [복구]
3. 안전점검 관리 → 신규 일정 등록 → Task 없이 저장 → 회차 상세 → [삭제] 버튼 노출 및 클릭 시 목록에서 사라짐
4. 안전점검 관리 → Task 있는 회차는 [삭제] 대신 기존 [점검 취소] 노출 확인
5. 점검 시작 → 결과 라디오가 양호/불량 2개만 노출(오동작 없음), 점검사진(선택) 업로드 필드 노출
6. 안전점검 관리 상단에 [오동작 등록] 버튼 사라짐, 작업 조치 관리에 오동작 필터/행 없음
7. 보고서 센터 → "점검결과 보고" 섹션에서 PDF 다운로드(사진 없어도 에러 없이 생성), 별지9 섹션 사라짐

preview 불안정 시 Task 1~10의 헤드리스 스모크 결과로 대체하고, 사용자에게 Ctrl+F5로 직접 확인 요청.

- [ ] **Step 2: PRD.md 로드맵에 v1.9 항목 추가**

`## 11. 로드맵`의 `### v1.8` 다음에 삽입:

```markdown
### v1.9 — 260907 현장요구사항 (2026-09)
- [x] 가설컨테이너용 신규 층(TEMP) — placeholder 배치도, 실측 배치도 수령 시 교체 예정
- [x] 시설관리 장비 소프트 삭제 (Equipment.active) + 안전점검 회차 원클릭 삭제
- [x] 점검유형 카탈로그 5종 → 3종 (일일/월간/특별) — 데이터 이관 SQL 별도 실행 필요
- [x] 점검결과 보고(사진대지) 신규 PDF — 별지5 외 범용 사진 증빙
- [x] 별지9(오동작 관리대장) UI 제거 — 전기팀 수기 관리로 전환, DB는 보존
```

세부 스펙은 `## 260907 개발내용` 섹션 참조하도록 각 6.x 절 관련 서술에 짧게 링크 문구 추가(예: 6.3 안전점검 관리 절 상단에 "점검유형 카탈로그는 260907 개발내용 참조" 한 줄).

- [ ] **Step 3: Commit**

```bash
git add PRD.md
git commit -m "$(cat <<'EOF'
docs(prd): 260907 현장요구사항 반영 — v1.9 로드맵 동기화

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
EOF
)"
```

(push는 사용자가 요청할 때 별도로 진행.)

## Self-Review 결과

- **스펙 커버리지**: PRD `## 260907 개발내용`의 5개 항목 모두 매핑됨 — 260907-1(Task 1), 260907-2(Task 3-6), 260907-3(Task 7), 260907-4(Task 2·8·9), 260907-5(Task 10). Task 11은 검증+문서화.
- **Placeholder 스캔**: 실측 배치도 미수령 상태를 반영한 Task 1의 placeholder 이미지는 의도된 임시 산출물(스펙에 "임시 스케치로 시작" 명시)이라 결함 아님. 그 외 "TBD"류 표현 없음.
- **타입 일관성**: `retire_equipment/restore_equipment`(Task 3) ↔ 호출부(Task 4) 일치. `delete_round(round_id, by) -> bool`(Task 5) ↔ 호출부(Task 6) 일치. `Deficiency.photo_path/inspection_photo_path`(Task 8) ↔ `_build_pdf_inspection_photo_report`(Task 9)에서 참조하는 필드명 일치.
- **비가역 작업 격리**: Task 7의 데이터 이관 SQL은 코드 변경(Step 1-2, 6)과 분리해 별도 사용자 승인 후 실행하도록 명시.
- **DB 보존 원칙**: Task 10에서 malfunctions 테이블/데이터 계층 함수는 삭제하지 않음을 각 Step과 커밋 메시지에 명시.
