# 장소 추가 기능 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 관리자가 "위치 마스터" 화면에서 PDF 도면을 업로드해 새 장소(건물/구역 단위)를 앱 재배포 없이 추가하고, 그 장소가 앱 전역의 모든 층 선택 목록에 기존 장소와 동일하게 나타나게 한다.

**Architecture:** 신규 Supabase 테이블 `floors`(code/display_name/image_path/sort_order) + 신규 Storage 버킷 `floor-plans`. 기존 8개 층(PIT~Roof)+TEMP는 `lib/data.py`의 `CORE_FLOORS` 상수로 그대로 유지(로컬 `assets/floors/*.png`)하고, 신규로 추가되는 커스텀 장소만 DB+Storage에 저장한다. `lib/data.py`에 `load_all_floors()` 단일 진입점을 만들어 기존 3곳의 하드코딩된 층 목록 상수(`LOCATION_FLOORS`/`ADMIN_FLOORS`/`SPOT_FLOORS`)를 대체한다.

**Tech Stack:** Streamlit, Supabase (Postgres + Storage), pymupdf(fitz)로 PDF→PNG 렌더링(기존 `scripts/convert_floor_pdfs.py`와 동일한 DPI 180 설정, 신규 의존성 없음 — `requirements.txt`에 이미 `pymupdf>=1.24.0` 있음).

**Spec:** `docs/superpowers/specs/2026-09-10-장소추가-design.md`

## Global Constraints

- DDL(테이블 생성)과 Storage 버킷 생성은 Claude가 직접 실행할 수 없다(Claude Code 자동 분류기가 차단) — 정확한 SQL/절차를 사용자에게 제시하고 Supabase 대시보드/SQL Editor에서 직접 실행하도록 안내한다.
- 마이그레이션 전에도 앱이 절대 크래시하면 안 된다 — 모든 `floors` 테이블 접근은 `floors_table_supported()` 프로브로 게이팅한다(기존 `*_supported()` 컨벤션).
- 기존 8개 층 + TEMP의 삭제·이름변경은 이번 작업 범위 밖 — 손대지 않는다.
- `pages_app/admin_center.py`의 위치 마스터 층 선택은 기존에 TEMP를 제외해 왔다(`ADMIN_FLOORS`에 TEMP 없음) — 이 동작을 그대로 유지한다(`load_all_floors(include_temp=False)`).
- 이 저장소에는 앱 코드용 pytest 스위트가 없다(`tests/`는 `db_monitor.py` 전용). 기존 세션 컨벤션을 따라 `python -c "import ast; ast.parse(...)"` 문법 체크 + 실제 라이브 Supabase DB에 대한 헤드리스 스모크 스크립트로 검증한다.
- 커밋 메시지 끝에 다음을 추가한다:
  ```
  Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du
  ```

---

## Task 1: DB 마이그레이션 SQL 작성 + supabase_schema.sql 동기화

**Files:**
- Create: `docs/superpowers/migrations/2026-09-10-floors-table.sql`
- Modify: `supabase_schema.sql` (파일 끝에 추가)

**Interfaces:**
- Produces: `public.floors` 테이블 스키마(code text PK, display_name text, image_path text, sort_order int, created_at timestamptz) — Task 2 이후 모든 DB 접근 코드가 이 스키마를 전제로 한다.

- [ ] **Step 1: 마이그레이션 SQL 파일 작성**

`docs/superpowers/migrations/2026-09-10-floors-table.sql`:

```sql
-- 장소 추가 기능 (2026-09-10) — 관리자가 앱에서 직접 등록하는 커스텀 장소.
-- 기존 8개 층(PIT/B2/B1/1F/2F/3F/4F/Roof) + TEMP는 코드 상수로 유지되며
-- 이 테이블에 들어가지 않는다. 여기엔 신규로 추가되는 장소만 저장된다.
create table if not exists public.floors (
  code         text primary key,
  display_name text not null,
  image_path   text not null,
  sort_order   int  not null,
  created_at   timestamptz not null default now()
);
```

- [ ] **Step 2: supabase_schema.sql에 동일 내용 동기화**

`supabase_schema.sql` 파일 맨 끝에 추가:

```sql

-- 9) 장소 추가 기능 (2026-09-10) — 관리자가 앱에서 직접 등록하는 커스텀 장소.
create table if not exists public.floors (
  code         text primary key,
  display_name text not null,
  image_path   text not null,
  sort_order   int  not null,
  created_at   timestamptz not null default now()
);
```

- [ ] **Step 3: 두 파일 내용이 동일한지 확인**

Run: `diff <(tail -8 docs/superpowers/migrations/2026-09-10-floors-table.sql) <(tail -8 supabase_schema.sql)`
Expected: `create table` 이하 본문이 동일(주석 줄 번호 차이는 무관, `create table if not exists public.floors (` 부터 마지막 `);` 까지 문자 그대로 일치해야 함).

- [ ] **Step 4: 커밋**

```bash
git add docs/superpowers/migrations/2026-09-10-floors-table.sql supabase_schema.sql
git commit -m "chore(db): floors 테이블 마이그레이션 SQL (장소 추가 기능)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du"
```

---

## Task 2: lib/data.py — 상수 + floors_table_supported() 프로브

**Files:**
- Modify: `lib/data.py:11` (import 추가), `lib/data.py:33` 부근 (상수 추가)

**Interfaces:**
- Consumes: 없음 (신규 최하위 계층)
- Produces:
  - `CORE_FLOORS: list[str]` — `["PIT", "B2", "B1", "1F", "2F", "3F", "4F", "Roof", "TEMP"]`
  - `FLOOR_PLAN_BUCKET: str` — `"floor-plans"`
  - `floors_table_supported() -> bool`

- [ ] **Step 1: pathlib import 추가**

`lib/data.py:11` 근처, 기존 임포트 블록:

```python
from dataclasses import dataclass
from datetime import date, datetime, timedelta
from typing import Literal
```

를 아래로 교체:

```python
from dataclasses import dataclass
from datetime import date, datetime, timedelta
from pathlib import Path
from typing import Literal
```

- [ ] **Step 2: 상수 + 프로브 함수 추가**

`lib/data.py:33` 부근, 기존:

```python
ACTION_PHOTO_BUCKET = "action-photos"
```

바로 아래에 추가:

```python
ACTION_PHOTO_BUCKET = "action-photos"
FLOOR_PLAN_BUCKET = "floor-plans"

# 기존 8개 층(PDF 도면 기준) + TEMP(가설동). 로컬 assets/floors/*.png로 제공되며
# 이 리스트는 바뀌지 않는다 — 새로 추가되는 장소는 floors 테이블에 저장된다.
CORE_FLOORS = ["PIT", "B2", "B1", "1F", "2F", "3F", "4F", "Roof", "TEMP"]
_ASSETS_FLOORS_DIR = Path(__file__).resolve().parent.parent / "assets" / "floors"


def floors_table_supported() -> bool:
    """floors 테이블 마이그레이션이 반영됐는지 확인 (probe). 미반영이면 False —
    호출부는 '장소 추가' UI를 비활성화해 앱이 크래시하지 않게 한다."""
    try:
        _db().table("floors").select("code").limit(1).execute()
        return True
    except Exception:
        return False
```

- [ ] **Step 3: 문법 확인**

Run: `python -c "import ast; ast.parse(open('lib/data.py', encoding='utf-8').read()); print('AST_OK')"`
Expected: `AST_OK`

- [ ] **Step 4: 헤드리스 스모크 (마이그레이션 전이어도 안전한지 확인)**

Run:
```bash
python -c "
from lib import data
print('floors_table_supported:', data.floors_table_supported())
print('CORE_FLOORS:', data.CORE_FLOORS)
"
```
Expected: 마이그레이션 전이면 `floors_table_supported: False`, 예외 없이 종료. (사용자가 Task 1의 SQL을 이미 실행했다면 `True`가 나와도 정상.)

- [ ] **Step 5: 커밋**

```bash
git add lib/data.py
git commit -m "feat(data): 장소 추가용 CORE_FLOORS/FLOOR_PLAN_BUCKET 상수 + floors_table_supported 프로브

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du"
```

---

## Task 3: lib/data.py — _floor_rows() 캐시 + load_all_floors() + floor_display_name()

**Files:**
- Modify: `lib/data.py` (Task 2에서 추가한 블록 바로 아래, 그리고 기존 `# ---------- 조회 (캐시) ----------` 섹션 부근에 캐시 함수 추가)

**Interfaces:**
- Consumes: `CORE_FLOORS`, `floors_table_supported()` (Task 2)
- Produces:
  - `_floor_rows() -> list[dict]` (각 dict: `code`, `display_name`, `image_path`, `sort_order`, `created_at`)
  - `load_all_floors(include_temp: bool = True) -> list[str]`
  - `floor_display_name(code: str) -> str`

- [ ] **Step 1: _floor_rows() 캐시 함수 추가**

기존 `# ---------- 조회 (캐시) ----------` 섹션의 다른 `_xxx_rows()` 함수들 바로 아래에 추가 (예: `_notice_rows()` 다음):

```python
@st.cache_data(ttl=_CACHE_TTL)
def _floor_rows() -> list[dict]:
    """floors 테이블 전체 (sort_order 순). 마이그레이션 전이면 빈 리스트."""
    try:
        return (_db().table("floors").select("*")
                .order("sort_order").execute().data)
    except Exception:
        return []
```

- [ ] **Step 2: load_all_floors() / floor_display_name() 추가**

Task 2에서 추가한 `floors_table_supported()` 함수 바로 아래에 추가:

```python
def load_all_floors(include_temp: bool = True) -> list[str]:
    """전체 장소 code 목록 — CORE_FLOORS + DB에 등록된 커스텀 장소(sort_order 순).
    include_temp=False면 CORE_FLOORS 중 TEMP를 제외한다
    (위치 마스터 화면이 기존에 TEMP를 안 보여주던 동작을 그대로 유지)."""
    core = [f for f in CORE_FLOORS if include_temp or f != "TEMP"]
    custom = [r["code"] for r in _floor_rows()]
    return core + custom


def floor_display_name(code: str) -> str:
    """장소 code → 화면 표시명. CORE_FLOORS는 code 자체가 표시명이다."""
    if code in CORE_FLOORS:
        return code
    for r in _floor_rows():
        if r["code"] == code:
            return r["display_name"]
    return code
```

- [ ] **Step 3: 문법 확인**

Run: `python -c "import ast; ast.parse(open('lib/data.py', encoding='utf-8').read()); print('AST_OK')"`
Expected: `AST_OK`

- [ ] **Step 4: 헤드리스 스모크**

Run:
```bash
python -c "
from lib import data
floors = data.load_all_floors()
print('load_all_floors():', floors)
assert floors[:9] == data.CORE_FLOORS, 'CORE_FLOORS가 앞에 그대로 있어야 함'
floors_no_temp = data.load_all_floors(include_temp=False)
assert 'TEMP' not in floors_no_temp, 'include_temp=False면 TEMP 빠져야 함'
print('floor_display_name(\'PIT\'):', data.floor_display_name('PIT'))
assert data.floor_display_name('PIT') == 'PIT'
print('OK')
"
```
Expected: 예외 없이 `OK` 출력. `load_all_floors()`의 앞 9개가 `CORE_FLOORS`와 동일.

- [ ] **Step 5: 커밋**

```bash
git add lib/data.py
git commit -m "feat(data): load_all_floors/floor_display_name — 층 목록 단일 진입점

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du"
```

---

## Task 4: lib/data.py — get_floor_image_bytes()

**Files:**
- Modify: `lib/data.py`

**Interfaces:**
- Consumes: `CORE_FLOORS`, `_ASSETS_FLOORS_DIR`, `_floor_rows()`, `FLOOR_PLAN_BUCKET` (Task 2, 3)
- Produces: `get_floor_image_bytes(code: str) -> bytes | None`

- [ ] **Step 1: 함수 추가**

`floor_display_name()` 바로 아래에 추가:

```python
def get_floor_image_bytes(code: str) -> bytes | None:
    """장소 도면 PNG bytes. CORE_FLOORS는 로컬 assets/floors/,
    커스텀 장소는 Storage(floor-plans 버킷)에서 가져온다. 실패 시 None."""
    if code in CORE_FLOORS:
        p = _ASSETS_FLOORS_DIR / f"{code}.png"
        return p.read_bytes() if p.exists() else None
    for r in _floor_rows():
        if r["code"] == code:
            try:
                return _db().storage.from_(FLOOR_PLAN_BUCKET).download(r["image_path"])
            except Exception:
                return None
    return None
```

- [ ] **Step 2: 문법 확인**

Run: `python -c "import ast; ast.parse(open('lib/data.py', encoding='utf-8').read()); print('AST_OK')"`
Expected: `AST_OK`

- [ ] **Step 3: 헤드리스 스모크 (CORE_FLOORS 이미지 로드 확인)**

Run:
```bash
python -c "
from lib import data
img = data.get_floor_image_bytes('1F')
print('1F image bytes:', len(img) if img else None)
assert img is not None and len(img) > 1000, '기존 1F 도면이 로드돼야 함'
assert data.get_floor_image_bytes('NOPE-CODE') is None
print('OK')
"
```
Expected: `1F` 이미지 바이트 길이 출력, `OK`. (이 저장소에 `assets/floors/1F.png`가 이미 존재하는 전제.)

- [ ] **Step 4: 커밋**

```bash
git add lib/data.py
git commit -m "feat(data): get_floor_image_bytes — 로컬/Storage 겸용 도면 이미지 조회

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du"
```

---

## Task 5: lib/data.py — render_floor_pdf_preview() / preview_floor_code() / add_floor()

**Files:**
- Modify: `lib/data.py`

**Interfaces:**
- Consumes: `CORE_FLOORS`, `_floor_rows()`, `FLOOR_PLAN_BUCKET` (Task 2, 3)
- Produces:
  - `render_floor_pdf_preview(pdf_bytes: bytes) -> bytes` (PNG bytes, 저장 안 함)
  - `preview_floor_code(display_name: str) -> str` (저장 안 함, 미리보기용)
  - `add_floor(display_name: str, pdf_bytes: bytes) -> str` (반환값: 새로 생성된 code)

- [ ] **Step 1: PDF 렌더링 + code 생성 헬퍼 추가**

`get_floor_image_bytes()` 바로 아래에 추가:

```python
def render_floor_pdf_preview(pdf_bytes: bytes) -> bytes:
    """PDF 1페이지를 PNG bytes로 렌더링 (미리보기용, 아무것도 저장하지 않음).
    scripts/convert_floor_pdfs.py와 동일하게 DPI 180 사용."""
    import fitz  # 이 함수를 쓸 때만 필요한 무거운 의존성이라 지연 import

    doc = fitz.open(stream=pdf_bytes, filetype="pdf")
    try:
        page = doc.load_page(0)
        matrix = fitz.Matrix(180 / 72, 180 / 72)
        pix = page.get_pixmap(matrix=matrix, alpha=False)
        return pix.tobytes("png")
    finally:
        doc.close()


def _floor_code_from_name(display_name: str, existing_codes: set[str]) -> str:
    """표시명 → 장소 code. 영문/숫자만 추출, 대문자화, 최대 12자.
    비어있으면 PLACE로 폴백. 기존 code와 충돌하면 숫자 접미사를 붙인다."""
    import re

    raw = re.sub(r"[^A-Za-z0-9]", "", display_name).upper()[:12] or "PLACE"
    if raw not in existing_codes:
        return raw
    n = 2
    while True:
        suffix = str(n)
        candidate = raw[: 12 - len(suffix)] + suffix
        if candidate not in existing_codes:
            return candidate
        n += 1


def preview_floor_code(display_name: str) -> str:
    """장소 추가 확정 전, 자동 생성될 code를 미리 계산 (저장하지 않음)."""
    existing = set(CORE_FLOORS) | {r["code"] for r in _floor_rows()}
    return _floor_code_from_name(display_name.strip(), existing)
```

- [ ] **Step 2: add_floor() 추가**

`preview_floor_code()` 바로 아래에 추가:

```python
def add_floor(display_name: str, pdf_bytes: bytes) -> str:
    """PDF 1페이지를 PNG로 렌더링해 Storage(floor-plans 버킷)에 올리고
    floors 테이블에 등록한다. 반환값은 새로 생성된 code.
    Storage 업로드나 DB insert가 실패하면 예외를 그대로 올린다
    (이미지만 올라가고 테이블엔 없는 반쪽 상태를 만들지 않기 위해,
    업로드를 먼저 하고 insert가 실패하면 업로드분 정리는 하지 않는다 —
    같은 code로 재시도하면 upsert로 덮어써지므로 안전하다)."""
    name = display_name.strip()
    if not name:
        raise ValueError("표시명을 입력해 주세요.")

    png_bytes = render_floor_pdf_preview(pdf_bytes)

    existing = set(CORE_FLOORS) | {r["code"] for r in _floor_rows()}
    code = _floor_code_from_name(name, existing)
    image_path = f"{code}.png"

    _db().storage.from_(FLOOR_PLAN_BUCKET).upload(
        image_path, png_bytes,
        {"content-type": "image/png", "upsert": "true"},
    )

    rows = _floor_rows()
    max_order = max([r["sort_order"] for r in rows], default=len(CORE_FLOORS) - 1)
    _db().table("floors").insert({
        "code": code,
        "display_name": name,
        "image_path": image_path,
        "sort_order": max_order + 1,
    }).execute()
    _floor_rows.clear()
    return code


def rename_floor(code: str, new_display_name: str) -> None:
    """커스텀 장소의 표시명 수정. CORE_FLOORS 대상이면 에러."""
    if code in CORE_FLOORS:
        raise ValueError(f"{code}는 기본 층이라 이름을 바꿀 수 없습니다.")
    name = new_display_name.strip()
    if not name:
        raise ValueError("표시명을 입력해 주세요.")
    _db().table("floors").update({"display_name": name}).eq("code", code).execute()
    _floor_rows.clear()
```

- [ ] **Step 3: 문법 확인**

Run: `python -c "import ast; ast.parse(open('lib/data.py', encoding='utf-8').read()); print('AST_OK')"`
Expected: `AST_OK`

- [ ] **Step 4: code 생성 로직 단위 스모크 (DB/Storage 호출 없이)**

Run:
```bash
python -c "
from lib.data import _floor_code_from_name

assert _floor_code_from_name('별관 2층', set()) == 'PLACE'  # 한글만이면 폴백
assert _floor_code_from_name('Annex 2F', set()) == 'ANNEX2F'
assert _floor_code_from_name('Annex 2F', {'ANNEX2F'}) == 'ANNEX2F2'
assert _floor_code_from_name('PIT', {'PIT'}) == 'PIT2'
print('OK')
"
```
Expected: 예외 없이 `OK`.

- [ ] **Step 5: (마이그레이션이 이미 반영된 환경이면) 실제 add_floor 왕복 스모크**

이 단계는 사용자가 Task 1의 SQL과 `floor-plans` Storage 버킷을 이미 만들어둔 경우에만 실행한다. `data.floors_table_supported()`가 `False`면 건너뛰고 Task 6으로 진행한다.

Run:
```bash
python -c "
import fitz
from lib import data

if not data.floors_table_supported():
    print('SKIP: floors 테이블 마이그레이션 미반영')
else:
    doc = fitz.open()
    page = doc.new_page(width=200, height=100)
    pdf_bytes = doc.tobytes()
    doc.close()

    code = data.add_floor('스모크테스트장소', pdf_bytes)
    print('added code:', code)
    assert code in data.load_all_floors()
    img = data.get_floor_image_bytes(code)
    assert img is not None and len(img) > 0

    data.rename_floor(code, '스모크테스트장소(수정됨)')
    assert data.floor_display_name(code) == '스모크테스트장소(수정됨)'

    # 정리 — 테스트 데이터 삭제
    data._db().table('floors').delete().eq('code', code).execute()
    data._db().storage.from_(data.FLOOR_PLAN_BUCKET).remove([f'{code}.png'])
    data._floor_rows.clear()
    print('OK (cleaned up)')
"
```
Expected: `floors_table_supported()`가 `False`면 `SKIP` 출력 후 정상 종료. `True`면 `added code: ...` → `OK (cleaned up)`까지 출력, 예외 없음.

- [ ] **Step 6: 커밋**

```bash
git add lib/data.py
git commit -m "feat(data): add_floor/rename_floor/preview_floor_code — 장소 추가 핵심 로직

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du"
```

---

## Task 6: admin_center.py — "+ 장소 추가" 버튼 + 다이얼로그

**Files:**
- Modify: `pages_app/admin_center.py:19-32` (상수/헬퍼 정리는 Task 7에서), `pages_app/admin_center.py:555-576` (`_spot_master_tab` 헤더), 새 함수 `_floor_add_dialog` 추가

**Interfaces:**
- Consumes: `data.floors_table_supported()`, `data.render_floor_pdf_preview()`, `data.preview_floor_code()`, `data.add_floor()` (Task 2, 5)
- Produces: `_floor_add_dialog()` — Task 8에서 재사용하지 않으므로 이 파일 내부 전용

- [ ] **Step 1: `_floor_add_dialog()` 함수 추가**

`_spot_define_dialog()` 함수(약 350번째 줄) 바로 위에 새 함수 추가:

```python
@st.dialog("장소 추가", width="large")
def _floor_add_dialog() -> None:
    """PDF 도면 업로드 → 표시명 입력 → 확정 전 렌더링 미리보기 → 저장."""
    st.markdown(
        "<div style='color:#64748B; font-size:0.9rem; margin-bottom:0.5rem;'>"
        "PDF 도면 1장을 업로드하면 1페이지를 이미지로 변환해 미리 보여줍니다. "
        "확인 후 확정하면 다른 장소들처럼 층 선택 목록에 바로 나타납니다."
        "</div>",
        unsafe_allow_html=True,
    )

    pdf_file = st.file_uploader("PDF 도면 *", type=["pdf"], key="floor_add_pdf")
    default_name = pdf_file.name.rsplit(".", 1)[0] if pdf_file is not None else ""
    display_name = st.text_input(
        "표시명 *",
        value=st.session_state.get("floor_add_name", default_name),
        key="floor_add_name",
        placeholder="예: 별관 2층",
    )

    preview_png = None
    if pdf_file is not None:
        try:
            preview_png = data.render_floor_pdf_preview(pdf_file.getvalue())
        except Exception as e:
            st.error(f"PDF를 읽지 못했습니다: {e}")
        if preview_png is not None:
            st.image(preview_png, caption="미리보기 (아직 저장되지 않음)",
                     use_container_width=True)
            preview_code = data.preview_floor_code(display_name.strip() or default_name)
            st.caption(f"생성될 코드: `{preview_code}`")

    bcol1, bcol2 = st.columns([1, 1])
    with bcol1:
        if st.button("취소", use_container_width=True, key="floor_add_cancel"):
            for k in ("floor_add_pdf", "floor_add_name"):
                st.session_state.pop(k, None)
            st.rerun()
    with bcol2:
        submit_disabled = pdf_file is None or not display_name.strip()
        if st.button("확정", type="primary", use_container_width=True,
                     key="floor_add_submit", disabled=submit_disabled):
            try:
                code = data.add_floor(display_name.strip(), pdf_file.getvalue())
            except Exception as e:
                st.error(f"장소 추가에 실패했습니다: {e}")
            else:
                for k in ("floor_add_pdf", "floor_add_name"):
                    st.session_state.pop(k, None)
                st.session_state["admin_spot_just_added"] = f"장소 '{code}' 추가 완료."
                st.rerun()
```

- [ ] **Step 2: `_spot_master_tab()` 헤더에 버튼 배치**

`pages_app/admin_center.py`의 기존:

```python
def _spot_master_tab() -> None:
    # 상단 헤더 + [+ 신규 위치 추가] 버튼
    head_l, head_r = st.columns([3, 1])
    with head_l:
        st.markdown(
            "<div style='color:#64748B; font-size:0.92rem;'>"
            "등록된 위치(spot)를 <b>관리·검증</b>합니다 — 사용 여부 확인, 신규(임시) 위치 "
            "정식 전환, 속성·좌표 조정.<br>"
            "<span style='color:#94A3B8; font-size:0.85rem;'>"
            "일상 신규 위치는 <b>시설 관리(장비 등록)</b>·<b>안전점검(신규 위치 점검)</b>에서 "
            "생성됩니다. 아래 버튼은 초기 세팅·예외용.</span>"
            "</div>",
            unsafe_allow_html=True,
        )
    with head_r:
        if st.button("+ 신규 위치 등록", type="secondary",
                     use_container_width=True, key="admin_spot_open_dlg"):
            for k in ("admin_spot_room", "admin_spot_notes",
                      "admin_spot_x_input", "admin_spot_y_input"):
                st.session_state.pop(k, None)
            _spot_define_dialog()
```

를 아래로 교체:

```python
def _spot_master_tab() -> None:
    # 상단 헤더 + [+ 장소 추가] + [+ 신규 위치 추가] 버튼
    head_l, head_r = st.columns([3, 1.6])
    with head_l:
        st.markdown(
            "<div style='color:#64748B; font-size:0.92rem;'>"
            "등록된 위치(spot)를 <b>관리·검증</b>합니다 — 사용 여부 확인, 신규(임시) 위치 "
            "정식 전환, 속성·좌표 조정.<br>"
            "<span style='color:#94A3B8; font-size:0.85rem;'>"
            "일상 신규 위치는 <b>시설 관리(장비 등록)</b>·<b>안전점검(신규 위치 점검)</b>에서 "
            "생성됩니다. 아래 버튼은 초기 세팅·예외용.</span>"
            "</div>",
            unsafe_allow_html=True,
        )
    with head_r:
        btn_place, btn_spot = st.columns(2)
        with btn_place:
            floors_ready = data.floors_table_supported()
            if st.button("+ 장소 추가", type="secondary",
                         use_container_width=True, key="admin_floor_open_dlg",
                         disabled=not floors_ready):
                _floor_add_dialog()
            if not floors_ready:
                st.caption("DB 마이그레이션 필요")
        with btn_spot:
            if st.button("+ 신규 위치 등록", type="secondary",
                         use_container_width=True, key="admin_spot_open_dlg"):
                for k in ("admin_spot_room", "admin_spot_notes",
                          "admin_spot_x_input", "admin_spot_y_input"):
                    st.session_state.pop(k, None)
                _spot_define_dialog()
```

- [ ] **Step 3: 문법 확인**

Run: `python -c "import ast; ast.parse(open('pages_app/admin_center.py', encoding='utf-8').read()); print('AST_OK')"`
Expected: `AST_OK`

- [ ] **Step 4: 커밋**

```bash
git add pages_app/admin_center.py
git commit -m "feat(admin): 위치 마스터에 '+ 장소 추가' 버튼 + 다이얼로그

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du"
```

---

## Task 7: admin_center.py — ADMIN_FLOORS/_floor_image_uri 리팩터 + 표시명 인라인 수정

**Files:**
- Modify: `pages_app/admin_center.py:6-32` (import/상수 정리), `pages_app/admin_center.py` 내 `ADMIN_FLOORS` 참조 3곳(`_spot_define_dialog`, `_spot_master_floor_preview`), `_spot_master_tab()` (표시명 수정 UI 추가)

**Interfaces:**
- Consumes: `data.load_all_floors()`, `data.get_floor_image_bytes()`, `data.floor_display_name()`, `data.rename_floor()`, `data.CORE_FLOORS` (Task 3, 4, 5)
- Produces: 없음 (터미널 리팩터 — 이 파일을 쓰는 다른 코드 없음)

- [ ] **Step 1: import/상수 정리**

기존:

```python
import base64
from pathlib import Path

import plotly.graph_objects as go
import streamlit as st

from lib import auth, data
from lib.data import Spot
from lib.floor_widget import control_toggle, legend_html, plotly_config
from lib.ui import badge, page_header

# 새 8개 층 (대시보드 Location 탭과 동일 순서)
ADMIN_FLOORS = ["PIT", "B2", "B1", "1F", "2F", "3F", "4F", "Roof"]
ASSETS_FLOORS_DIR = Path(__file__).resolve().parent.parent / "assets" / "floors"

# 도면 PNG 모두 동일 픽셀 크기 (convert_floor_pdfs.py 기준)
FIG_W = 2978
FIG_H = 2105


def _floor_image_uri(floor: str) -> str | None:
    p = ASSETS_FLOORS_DIR / f"{floor}.png"
    if not p.exists():
        return None
    return f"data:image/png;base64,{base64.b64encode(p.read_bytes()).decode('ascii')}"
```

를 아래로 교체:

```python
import base64

import plotly.graph_objects as go
import streamlit as st

from lib import auth, data
from lib.data import Spot
from lib.floor_widget import control_toggle, legend_html, plotly_config
from lib.ui import badge, page_header

# 도면 PNG 모두 동일 픽셀 크기로 취급 (convert_floor_pdfs.py 기준) — 신규 업로드
# 장소는 원본 PDF 비율에 따라 다를 수 있지만, stretch 렌더링이라 좌표(%)는 그대로 맞는다.
FIG_W = 2978
FIG_H = 2105


def _floor_image_uri(floor: str) -> str | None:
    img = data.get_floor_image_bytes(floor)
    if img is None:
        return None
    return f"data:image/png;base64,{base64.b64encode(img).decode('ascii')}"
```

- [ ] **Step 2: `_spot_define_dialog()`의 ADMIN_FLOORS 참조 교체**

기존:

```python
    floor = st.selectbox(
        "층 선택",
        options=ADMIN_FLOORS,
        key="admin_spot_dlg_floor",
    )
```

를 아래로 교체:

```python
    floor = st.selectbox(
        "층 선택",
        options=data.load_all_floors(include_temp=False),
        key="admin_spot_dlg_floor",
    )
```

- [ ] **Step 3: `_spot_master_floor_preview()`의 ADMIN_FLOORS 참조 2곳 교체**

기존:

```python
    all_spots = spots  # 이미 전체 로드됨
    by_floor: dict[str, list[Spot]] = {f: [] for f in ADMIN_FLOORS}
    for s in all_spots:
        by_floor.setdefault(s.floor, []).append(s)

    n_cols = 4
    for row_start in range(0, len(ADMIN_FLOORS), n_cols):
        row_floors = ADMIN_FLOORS[row_start:row_start + n_cols]
```

를 아래로 교체:

```python
    all_floors = data.load_all_floors(include_temp=False)
    all_spots = spots  # 이미 전체 로드됨
    by_floor: dict[str, list[Spot]] = {f: [] for f in all_floors}
    for s in all_spots:
        by_floor.setdefault(s.floor, []).append(s)

    n_cols = 4
    for row_start in range(0, len(all_floors), n_cols):
        row_floors = all_floors[row_start:row_start + n_cols]
```

- [ ] **Step 4: `_spot_master_tab()`의 층 선택 selectbox도 동적 목록 사용 + 표시명 인라인 수정 UI 추가**

기존:

```python
    floor = st.selectbox(
        "층 선택",
        options=["전체"] + list(ADMIN_FLOORS),
        key="admin_spot_floor",
    )
    spots = data.load_spots() if floor == "전체" else data.load_spots(floor)

    # --- 도면 미리보기 (읽기 전용, v1.7) ---
    _spot_master_floor_preview(floor, spots)
```

를 아래로 교체:

```python
    floor = st.selectbox(
        "층 선택",
        options=["전체"] + data.load_all_floors(include_temp=False),
        key="admin_spot_floor",
    )
    spots = data.load_spots() if floor == "전체" else data.load_spots(floor)

    # 커스텀 장소(DB에 등록된 것)만 표시명 수정 가능 — CORE_FLOORS는 코드 자체가 표시명
    if floor != "전체" and floor not in data.CORE_FLOORS:
        with st.expander(f"'{data.floor_display_name(floor)}' 표시명 수정",
                         expanded=False):
            new_name = st.text_input(
                "새 표시명",
                value=data.floor_display_name(floor),
                key=f"floor_rename_{floor}",
            )
            if st.button("저장", key=f"floor_rename_save_{floor}"):
                try:
                    data.rename_floor(floor, new_name)
                except Exception as e:
                    st.error(str(e))
                else:
                    st.success("표시명을 수정했습니다.")
                    st.rerun()

    # --- 도면 미리보기 (읽기 전용, v1.7) ---
    _spot_master_floor_preview(floor, spots)
```

- [ ] **Step 5: 문법 확인**

Run: `python -c "import ast; ast.parse(open('pages_app/admin_center.py', encoding='utf-8').read()); print('AST_OK')"`
Expected: `AST_OK`

- [ ] **Step 6: ADMIN_FLOORS가 더 이상 남아있지 않은지 확인**

Run: `grep -n "ADMIN_FLOORS" pages_app/admin_center.py`
Expected: 아무 출력 없음 (모두 교체됨).

- [ ] **Step 7: 커밋**

```bash
git add pages_app/admin_center.py
git commit -m "refactor(admin): ADMIN_FLOORS 하드코딩 제거 → data.load_all_floors() 사용

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du"
```

---

## Task 8: dashboard.py — LOCATION_FLOORS/_floor_image_uri 리팩터

**Files:**
- Modify: `pages_app/dashboard.py:1-16` (import/상수 정리), `pages_app/dashboard.py:128-134` (`_floor_image_uri`), `pages_app/dashboard.py` 내 `_grid_tab()` (약 352-388줄)

**Interfaces:**
- Consumes: `data.load_all_floors()`, `data.get_floor_image_bytes()` (Task 3, 4)
- Produces: 없음 (터미널 리팩터)

- [ ] **Step 1: import/상수 정리**

기존:

```python
"""대시보드 페이지 — 2탭 구조 (현황 요약 · 층별 도면)."""
from __future__ import annotations

import base64
from pathlib import Path

import plotly.graph_objects as go
import streamlit as st

from lib import data
from lib.ui import TASK_STATUS_KO, badge, fmt_date, page_header, render_kpi_row


# 새 8층 체계 (PDF 도면 기준) — Location 탭에서 사용
LOCATION_FLOORS = ["PIT", "B2", "B1", "1F", "2F", "3F", "4F", "Roof", "TEMP"]
ASSETS_FLOORS_DIR = Path(__file__).resolve().parent.parent / "assets" / "floors"
```

를 아래로 교체:

```python
"""대시보드 페이지 — 2탭 구조 (현황 요약 · 층별 도면)."""
from __future__ import annotations

import base64

import plotly.graph_objects as go
import streamlit as st

from lib import data
from lib.ui import TASK_STATUS_KO, badge, fmt_date, page_header, render_kpi_row
```

(`LOCATION_FLOORS`를 임포트해 쓰던 `pages_app/report_center.py`는 Task 10에서 별도로 고친다.)

- [ ] **Step 2: `_floor_image_uri()` 교체**

기존:

```python
def _floor_image_uri(floor: str) -> str | None:
    """assets/floors/{floor}.png를 data URI로 (plotly 백그라운드용)."""
    p = ASSETS_FLOORS_DIR / f"{floor}.png"
    if not p.exists():
        return None
    b64 = base64.b64encode(p.read_bytes()).decode("ascii")
    return f"data:image/png;base64,{b64}"
```

를 아래로 교체:

```python
def _floor_image_uri(floor: str) -> str | None:
    """장소 도면을 data URI로 (plotly 백그라운드용)."""
    img = data.get_floor_image_bytes(floor)
    if img is None:
        return None
    return f"data:image/png;base64,{base64.b64encode(img).decode('ascii')}"
```

- [ ] **Step 3: `_grid_tab()`의 LOCATION_FLOORS 참조 4곳 교체**

기존:

```python
def _grid_tab() -> None:
    """탭 2 — Location: 8개 층 카드 그리드 + 도면 모달."""
    eq_all = data.load_equipment()
    notices = data.load_notices()

    all_stats = {fl: _floor_stats(fl, eq_all, notices) for fl in LOCATION_FLOORS}
    total = sum(s["total"] for s in all_stats.values())
    fail_total = sum(s["fail"] for s in all_stats.values())
    due_total = sum(s["due"] for s in all_stats.values())
    pending_total = sum(s["pending_notices"] for s in all_stats.values())

    render_kpi_row([
        ("총 장비", f"{total}", f"{len(LOCATION_FLOORS)}개 층", "default"),
```

를 아래로 교체:

```python
def _grid_tab() -> None:
    """탭 2 — Location: 8개 층 카드 그리드 + 도면 모달."""
    floors = data.load_all_floors()
    eq_all = data.load_equipment()
    notices = data.load_notices()

    all_stats = {fl: _floor_stats(fl, eq_all, notices) for fl in floors}
    total = sum(s["total"] for s in all_stats.values())
    fail_total = sum(s["fail"] for s in all_stats.values())
    due_total = sum(s["due"] for s in all_stats.values())
    pending_total = sum(s["pending_notices"] for s in all_stats.values())

    render_kpi_row([
        ("총 장비", f"{total}", f"{len(floors)}개 층", "default"),
```

그리고 같은 함수 안, 기존:

```python
    for row_start in range(0, len(LOCATION_FLOORS), GRID_COLS):
        row = LOCATION_FLOORS[row_start:row_start + GRID_COLS]
```

를 아래로 교체:

```python
    for row_start in range(0, len(floors), GRID_COLS):
        row = floors[row_start:row_start + GRID_COLS]
```

- [ ] **Step 4: 문법 확인**

Run: `python -c "import ast; ast.parse(open('pages_app/dashboard.py', encoding='utf-8').read()); print('AST_OK')"`
Expected: `AST_OK`

- [ ] **Step 5: LOCATION_FLOORS가 더 이상 남아있지 않은지 확인**

Run: `grep -n "LOCATION_FLOORS" pages_app/dashboard.py`
Expected: 아무 출력 없음.

- [ ] **Step 6: 커밋**

```bash
git add pages_app/dashboard.py
git commit -m "refactor(dashboard): LOCATION_FLOORS 하드코딩 제거 → data.load_all_floors() 사용

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du"
```

---

## Task 9: inspection_dialog.py + equipment_inventory.py — SPOT_FLOORS 리팩터

**Files:**
- Modify: `lib/inspection_dialog.py:33-35` (상수 제거), `lib/inspection_dialog.py:2021` (참조 교체)
- Modify: `pages_app/equipment_inventory.py:8` (import 정리), `pages_app/equipment_inventory.py:616-617` (참조 교체)

**Interfaces:**
- Consumes: `data.load_all_floors()` (Task 3)
- Produces: 없음 (터미널 리팩터)

- [ ] **Step 1: `lib/inspection_dialog.py`의 SPOT_FLOORS 상수 제거**

기존(33-35줄 부근):

```python
EQ_FLOORS = ["B3", "B2", "B1", "P4", "L1", "L2", "2F", "4F", "5F", "6F", "SRV"]
# 실제 도면 PNG(assets/floors)가 있는 8개 층 — 신규 위치 생성 좌표 픽업용
SPOT_FLOORS = ["PIT", "B2", "B1", "1F", "2F", "3F", "4F", "Roof", "TEMP"]
```

를 아래로 교체 (EQ_FLOORS는 PRD에 기록된 레거시 코드라 유지):

```python
EQ_FLOORS = ["B3", "B2", "B1", "P4", "L1", "L2", "2F", "4F", "5F", "6F", "SRV"]
```

- [ ] **Step 2: `lib/inspection_dialog.py:2021`의 SPOT_FLOORS 참조 교체**

기존:

```python
            new_floor = st.selectbox("층", options=SPOT_FLOORS, key="eq_dlg_new_floor")
```

를 아래로 교체:

```python
            new_floor = st.selectbox("층", options=data.load_all_floors(), key="eq_dlg_new_floor")
```

- [ ] **Step 3: 문법 확인 (inspection_dialog.py)**

Run: `python -c "import ast; ast.parse(open('lib/inspection_dialog.py', encoding='utf-8').read()); print('AST_OK')"`
Expected: `AST_OK`

- [ ] **Step 4: `pages_app/equipment_inventory.py`의 import 정리**

기존:

```python
from lib.inspection_dialog import EQ_FLOORS, SPOT_FLOORS, equipment_dialog
```

를 아래로 교체:

```python
from lib.inspection_dialog import EQ_FLOORS, equipment_dialog
```

- [ ] **Step 5: `pages_app/equipment_inventory.py:616-617`의 SPOT_FLOORS 참조 교체**

기존:

```python
                extra = [f for f in sorted({e.floor for e in eq}) if f not in SPOT_FLOORS]
                eq_floors = SPOT_FLOORS + extra
```

를 아래로 교체:

```python
                all_floors = data.load_all_floors()
                extra = [f for f in sorted({e.floor for e in eq}) if f not in all_floors]
                eq_floors = all_floors + extra
```

- [ ] **Step 6: 문법 확인 (equipment_inventory.py)**

Run: `python -c "import ast; ast.parse(open('pages_app/equipment_inventory.py', encoding='utf-8').read()); print('AST_OK')"`
Expected: `AST_OK`

- [ ] **Step 7: SPOT_FLOORS가 두 파일 어디에도 남아있지 않은지 확인**

Run: `grep -rn "SPOT_FLOORS" lib/inspection_dialog.py pages_app/equipment_inventory.py`
Expected: 아무 출력 없음.

- [ ] **Step 8: 커밋**

```bash
git add lib/inspection_dialog.py pages_app/equipment_inventory.py
git commit -m "refactor(inspection,equipment): SPOT_FLOORS 하드코딩 제거 → data.load_all_floors() 사용

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du"
```

---

## Task 10: report_center.py — LOCATION_FLOORS import 리팩터

**Files:**
- Modify: `pages_app/report_center.py:868-880` (QR 스티커 섹션)

**Interfaces:**
- Consumes: `data.load_all_floors()` (Task 3) — 이 파일은 이미 `from lib import data`를 임포트하고 있다.
- Produces: 없음 (터미널 리팩터)

- [ ] **Step 1: LOCATION_FLOORS import + 참조 교체**

기존:

```python
    midq, _ = st.columns([2, 1])
    with midq:
        from pages_app.dashboard import LOCATION_FLOORS
        _scope_colq, _btn_colq = st.columns([2.2, 1.3], vertical_alignment="bottom")
        with _scope_colq:
            _sel_floor_q = st.selectbox(
                "출력 범위", ["전체 (모든 층)"] + list(LOCATION_FLOORS),
                key="qr_sticker_floor",
            )
```

를 아래로 교체:

```python
    midq, _ = st.columns([2, 1])
    with midq:
        _scope_colq, _btn_colq = st.columns([2.2, 1.3], vertical_alignment="bottom")
        with _scope_colq:
            _sel_floor_q = st.selectbox(
                "출력 범위", ["전체 (모든 층)"] + data.load_all_floors(),
                key="qr_sticker_floor",
            )
```

- [ ] **Step 2: 문법 확인**

Run: `python -c "import ast; ast.parse(open('pages_app/report_center.py', encoding='utf-8').read()); print('AST_OK')"`
Expected: `AST_OK`

- [ ] **Step 3: LOCATION_FLOORS가 더 이상 참조되지 않는지 확인**

Run: `grep -n "LOCATION_FLOORS" pages_app/report_center.py pages_app/dashboard.py`
Expected: 아무 출력 없음.

- [ ] **Step 4: 커밋**

```bash
git add pages_app/report_center.py
git commit -m "refactor(report): LOCATION_FLOORS 하드코딩 제거 → data.load_all_floors() 사용

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01RJ12frByT2uwNq4KTWj4Du"
```

---

## Task 11: 전체 앱 부팅 확인 + 사용자 마이그레이션 안내

**Files:** 없음 (검증 전용 — 코드 변경 없음)

**Interfaces:**
- Consumes: Task 1~10에서 만든 모든 함수/리팩터
- Produces: 없음 (최종 통합 검증)

- [ ] **Step 1: 6개 수정 파일 전체 문법 재확인**

Run:
```bash
for f in lib/data.py lib/inspection_dialog.py pages_app/admin_center.py pages_app/dashboard.py pages_app/equipment_inventory.py pages_app/report_center.py; do
  python -c "import ast; ast.parse(open('$f', encoding='utf-8').read())" || echo "SYNTAX ERROR in $f"
done
echo "ALL CHECKED"
```
Expected: `SYNTAX ERROR` 줄 없이 `ALL CHECKED`만 출력.

- [ ] **Step 2: 로컬 서버 재기동**

Run (Windows/Git Bash 환경, 기존 컨벤션):
```bash
netstat -ano | grep ":8501.*LISTENING"
```
떠 있는 PID가 있으면 `taskkill //F //PID <pid>` 로 종료 후:
```bash
streamlit run app.py --server.headless true --server.port 8501 > /tmp/streamlit_floor.log 2>&1 &
disown
sleep 4
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:8501
```
Expected: `HTTP 200`. `/tmp/streamlit_floor.log`에 `Traceback`이 없는지 `grep -i traceback /tmp/streamlit_floor.log`로 확인 (출력 없어야 정상).

- [ ] **Step 3: 브라우저로 실제 흐름 확인 (수동)**

1. 관리자 계정으로 로그인 → 관리자 메뉴 → 위치 마스터.
2. `floors_table_supported()`가 `False`인 상태(마이그레이션 전)라면 "+ 장소 추가" 버튼이 비활성화 + "DB 마이그레이션 필요" 캡션이 보이는지 확인.
3. 사용자가 Task 1의 SQL을 Supabase SQL Editor에서 실행하고, Supabase 대시보드에서 `floor-plans` Storage 버킷을 생성(Public 아님, 기존 `action-photos`와 동일 정책)한 뒤 앱을 새로고침.
4. "+ 장소 추가" 버튼이 활성화됐는지 확인 → 클릭 → 아무 PDF 1장 업로드 → 표시명 입력 → 미리보기 이미지와 생성될 코드가 뜨는지 확인 → "확정" 클릭.
5. 성공 토스트 확인 후, 층 선택 드롭다운(위치마스터 자체, 시설 관리, 안전점검 신규 위치, 보고서센터 QR스티커 출력범위, 대시보드 Location 탭 카드 그리드)에 새 장소가 나타나는지 각각 확인.
6. 위치 마스터에서 새로 추가한 장소를 선택 → "표시명 수정" 익스팬더로 이름을 바꾸고 저장 → 반영되는지 확인.

- [ ] **Step 4: 사용자에게 필요한 수동 작업 안내**

이 태스크를 실행하는 사람은 최종 사용자에게 아래를 안내한다 (Claude는 DDL/Storage 버킷 생성을 직접 실행할 수 없음, 기존 컨벤션):

- `docs/superpowers/migrations/2026-09-10-floors-table.sql` 내용을 Supabase SQL Editor에서 실행.
- Supabase 대시보드 → Storage에서 `floor-plans` 버킷 생성 (기존 `action-photos` 버킷과 동일한 접근 정책으로).

- [ ] **Step 5: 최종 커밋 (진행 로그가 있다면 갱신)**

이 태스크는 검증 전용이라 별도 커밋 대상 파일이 없으면 커밋을 생략한다. `.superpowers/sdd/progress.md` 등 진행 로그 파일을 쓰는 컨벤션이 있다면 이 시점에 "장소 추가 기능 구현 완료" 한 줄을 추가하고 커밋한다.
