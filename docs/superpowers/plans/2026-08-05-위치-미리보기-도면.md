# 장비 등록 '기존 위치 선택' 도면 미리보기 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 장비 등록 다이얼로그 "기존 위치 선택" 모드에, 드롭다운으로 고른 spot의 위치를 도면 위 마커로 보여주는 읽기전용·접이식 미리보기를 추가한다.

**Architecture:** 새 정적 렌더 함수 `_spot_preview_map(sel_spot)`를 추가(기존 인터랙티브 픽커의 좌표 매핑·마커 패턴 재사용, 클릭/잠금토글 없음)하고, "기존 위치 선택" 분기의 드롭다운 아래 `st.expander(expanded=False)` 안에서 호출한다. 순수 additive — 기존 픽커·"신규 위치 만들기"·DB 무변경.

**Tech Stack:** Python, Streamlit 1.51, Plotly(graph_objects), `lib/floor_widget` 헬퍼.

## Global Constraints

- 외부 라이브러리 추가 금지 (plotly·streamlit 이미 사용). 표준 자산만.
- 읽기전용: DB·스키마·저장 로직 변경 없음.
- 기존 `_location_map_picker`, `_eq_new_spot_map`("신규 위치 만들기"), 픽커 동작 **무영향** (건드리지 않음).
- 범위: 장비 등록 다이얼로그 "기존 위치 선택" 모드에만.
- pytest 없음 → 검증은 `python -c "import ast; ast.parse(...)"` (AST OK) + `import lib.inspection_dialog` (IMPORT OK) + 프리뷰 실화면.
- 좌표 매핑은 기존 픽커와 동일해야 함: `FIG_W, FIG_H = 2978, 2105`, `x = x_pct/100*FIG_W`, `y = FIG_H - y_pct/100*FIG_H`.
- 스펙: [docs/superpowers/specs/2026-08-05-위치-미리보기-도면-design.md](../specs/2026-08-05-위치-미리보기-도면-design.md)

---

## File Structure

- Modify: `lib/inspection_dialog.py`
  - **추가**: `_spot_preview_map(sel_spot)` — 읽기전용 도면 미리보기 렌더러 (기존 `_location_map_picker` 근처, 대략 line 738 이후).
  - **수정**: 장비 등록 다이얼로그 "기존 위치 선택" 분기([lib/inspection_dialog.py:1908-1940](../../../lib/inspection_dialog.py)) 끝에 expander 호출 3줄 추가.

`FloorSpot` 속성(참고): `floor, room_name, x_pct, y_pct, spot_id` (DB `floor_spots` 컬럼과 동일). `data.load_spots(floor)`는 층 필터 인자를 받고, `data.load_equipment()`는 전체 장비 반환(둘 다 캐시됨).

---

### Task 1: 읽기전용 도면 미리보기 추가 + 배치

**Files:**
- Modify: `lib/inspection_dialog.py` (함수 추가 + 다이얼로그 분기 3줄)

**Interfaces:**
- Produces: `_spot_preview_map(sel_spot) -> None` — `sel_spot`(FloorSpot)의 층 도면을 그려 선택 spot을 파란 별로 강조, 같은 층 다른 spot/장비를 옅은 회색으로 표시. 반환값 없음(렌더만). 클릭/선택/세션상태 변경 없음.
- Consumes: `data.load_spots(floor)`, `data.load_equipment()`, `lib.floor_widget.plotly_config`.

- [ ] **Step 1: `_spot_preview_map` 함수 추가**

`lib/inspection_dialog.py`에서 `_location_map_picker`의 `return st.session_state.get(picked_key)`로 끝나는 블록(대략 line 736) **다음 빈 줄 뒤**에 아래 함수를 추가한다:

```python
def _spot_preview_map(sel_spot) -> None:
    """'기존 위치 선택'에서 고른 spot 위치를 도면 위에 보여주는 읽기전용 미리보기.

    선택 spot = 파란 별(★), 같은 층의 다른 spot = 옅은 회색 ◇, 기존 장비 = 옅은 회색 ●.
    클릭·선택·세션상태 변경 없음(정적).
    """
    import base64
    from pathlib import Path
    import plotly.graph_objects as go
    from lib.floor_widget import plotly_config

    ASSETS_DIR = Path(__file__).resolve().parent.parent / "assets" / "floors"
    FIG_W, FIG_H = 2978, 2105

    floor = sel_spot.floor
    img_path = ASSETS_DIR / f"{floor}.png"
    if not img_path.exists():
        st.warning(f"{floor} 도면 이미지가 없습니다.")
        return
    uri = "data:image/png;base64," + base64.b64encode(img_path.read_bytes()).decode()

    spots = data.load_spots(floor)
    floor_eq = [e for e in data.load_equipment() if e.floor == floor and e.spot_id]
    spot_by_id = {s.spot_id: s for s in spots}

    def _xy(x_pct, y_pct):
        return x_pct / 100 * FIG_W, FIG_H - y_pct / 100 * FIG_H

    fig = go.Figure()
    fig.add_layout_image(dict(
        source=uri, xref="x", yref="y",
        x=0, y=FIG_H, sizex=FIG_W, sizey=FIG_H,
        sizing="stretch", layer="below", opacity=1.0,
    ))

    # 맥락 1: 같은 층의 다른 spot (선택 제외) — 옅은 회색 다이아
    ox, oy, ot = [], [], []
    for s in spots:
        if s.spot_id == sel_spot.spot_id:
            continue
        x, y = _xy(s.x_pct, s.y_pct)
        ox.append(x); oy.append(y); ot.append(f"{s.room_name} ({s.spot_id})")
    if ox:
        fig.add_trace(go.Scatter(
            x=ox, y=oy, mode="markers", text=ot,
            marker=dict(size=13, color="#94A3B8",
                        line=dict(color="#FFFFFF", width=1.5), symbol="diamond"),
            hovertemplate="%{text}<extra></extra>", showlegend=False,
        ))

    # 맥락 2: 같은 층 기존 장비 — 옅은 회색 원
    ex, ey, et = [], [], []
    for e in floor_eq:
        sp = spot_by_id.get(e.spot_id)
        if not sp:
            continue
        x, y = _xy(sp.x_pct, sp.y_pct)
        ex.append(x); ey.append(y); et.append(f"{e.equipment_name} ({e.equipment_id})")
    if ex:
        fig.add_trace(go.Scatter(
            x=ex, y=ey, mode="markers", text=et,
            marker=dict(size=11, color="#CBD5E1",
                        line=dict(color="#FFFFFF", width=1), symbol="circle"),
            hovertemplate="%{text}<extra></extra>", showlegend=False,
        ))

    # 선택 spot — 파란 별 강조
    sx, sy = _xy(sel_spot.x_pct, sel_spot.y_pct)
    fig.add_trace(go.Scatter(
        x=[sx], y=[sy], mode="markers",
        text=[f"{sel_spot.room_name} ({sel_spot.spot_id})"],
        marker=dict(size=24, color="#2563EB",
                    line=dict(color="#FFFFFF", width=2), symbol="star"),
        hovertemplate="<b>%{text}</b><extra></extra>", showlegend=False,
    ))

    fig.update_xaxes(visible=False, range=[0, FIG_W], constrain="domain")
    fig.update_yaxes(visible=False, range=[0, FIG_H], scaleanchor="x", scaleratio=1)
    fig.update_layout(
        margin=dict(l=0, r=0, t=0, b=0),
        plot_bgcolor="#F8FAFC", height=360,
        dragmode=False, showlegend=False,
        uirevision=f"spot_preview_{floor}",
    )
    st.plotly_chart(
        fig, use_container_width=True, config=plotly_config(),
        key=f"spot_preview_{sel_spot.spot_id}",
    )
```

- [ ] **Step 2: AST/IMPORT 검증 (함수 추가분)**

Run:
```bash
python -c "import ast; ast.parse(open('lib/inspection_dialog.py', encoding='utf-8').read()); print('AST OK')"
python -c "import lib.inspection_dialog; print('IMPORT OK')"
```
Expected: `AST OK` 그리고 `IMPORT OK` 둘 다 출력. (import 시 `plotly_config`가 floor_widget에 존재하는지도 함께 검증됨 — 없으면 실패)

- [ ] **Step 3: "기존 위치 선택" 분기에 expander 호출 추가**

[lib/inspection_dialog.py:1922-1940](../../../lib/inspection_dialog.py) 의 `with c4:` 블록(spot 드롭다운) 바로 다음, `if loc_mode == "기존 위치 선택":` 블록 안(8칸 들여쓰기)에 아래를 추가한다. 즉 `else:`(신규 위치 만들기, 현재 line 1941) **직전**에 삽입:

```python
        if sel_spot is not None:
            with st.expander("🗺️ 도면에서 위치 확인", expanded=False):
                _spot_preview_map(sel_spot)
```

확인: 이 블록은 `c3, c4 = st.columns([1, 2])`와 같은 들여쓰기(8칸)여야 하고, `else:`(신규 위치 만들기) 위에 위치해야 한다. `sel_spot`은 이 분기에서 spot 선택 시 채워지고, 미선택이면 `None`(도면 안 그림).

- [ ] **Step 4: AST/IMPORT 재검증 (배치분)**

Run:
```bash
python -c "import ast; ast.parse(open('lib/inspection_dialog.py', encoding='utf-8').read()); print('AST OK')"
python -c "import lib.inspection_dialog; print('IMPORT OK')"
```
Expected: `AST OK`, `IMPORT OK`.

- [ ] **Step 5: 프리뷰 실화면 검증 (컨트롤러 담당)**

프리뷰(pyrosafe-streamlit) 기동 후: 시설 관리 → 신규 장비 등록 → "위치 지정: 기존 위치 선택".
확인 항목:
1. 드롭다운 아래 **🗺️ 도면에서 위치 확인** expander가 **접힌 상태**로 나타남.
2. 펼치면 해당 층 도면 + 선택 spot에 **파란 별**, 주변 spot/장비는 **옅은 회색**.
3. `위치(spot)` 드롭다운을 바꾸면 별 위치가 즉시 이동.
4. `층`을 spot 없는 층(있다면)으로 바꾸거나 spot 미선택 시 도면 미표시(기존 안내 유지).
5. "신규 위치 만들기" 모드로 전환 시 기존 클릭 도면 그대로(무영향).
6. 콘솔/`preview_logs` 오류 없음.

(도면 이미지가 없는 층은 "…도면 이미지가 없습니다." 안내가 뜨는지 확인.)

- [ ] **Step 6: 커밋**

```bash
git add lib/inspection_dialog.py
git commit -m "feat(equipment): '기존 위치 선택'에 읽기전용 도면 미리보기(접이식) 추가"
```

---

## Self-Review

- **Spec coverage:** 읽기전용(✓ 정적, on_select 없음)·선택 spot 강조+주변 옅게(✓ 별/회색)·기본 접힘(✓ expanded=False)·"기존 위치 선택"만(✓ 해당 분기)·신규모드 무영향(✓ 미변경)·이미지 없음 안내(✓ Step1)·자기완결 로드(✓ data.load_*) 모두 Task 1에 포함.
- **Placeholder scan:** 실제 코드 전량 포함, TBD/TODO 없음.
- **Type consistency:** `_spot_preview_map(sel_spot)` 시그니처가 Step1 정의·Step3 호출·Interfaces에서 일치. FloorSpot 속성명(`floor/room_name/x_pct/y_pct/spot_id`)이 기존 픽커(line 695-697) 사용과 일치.
