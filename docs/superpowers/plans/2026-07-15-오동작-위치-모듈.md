# 오동작 위치 모듈 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 오동작 등록(별지9)에 도면 기반 위치(층/구역/spot) 선택을 붙이고, 통합 목록에서 오동작 행을 지적사항 행과 동일한 열 구조로 정렬한다.

**Architecture:** `malfunctions` 테이블에 nullable 위치 컬럼(floor/zone/spot_id)을 추가하고 `Malfunction` 모델을 확장한다. 회차 컨텍스트가 없는 범용 도면 픽커 `_location_map_picker`를 신설해 오동작 다이얼로그에 장착한다. 통합 목록(deficiency_manager)의 오동작 UnifiedRow 매핑을 `location=층/구역`, `category=소방시설 구분`으로 바꾼다.

**Tech Stack:** Streamlit 1.57+, Supabase(PostgREST), Plotly, Python 3.11. 스키마 변경은 Supabase MCP(`apply_migration`)로만 적용(서비스키 PostgREST는 DDL 불가).

## Global Constraints

- 스키마 DDL은 반드시 Supabase MCP `apply_migration`으로 적용 (프로젝트 `amyirlqfpwhcdmzzytoi` = Pyrosafe). PostgREST 서비스키로는 DDL 불가.
- 이 저장소엔 pytest 스위트가 없다. 검증은 ① `python -c "import ast; ast.parse(...)"` 구문, ② 헤드리스 `python -c "from lib import data; ..."` 라운드트립(라이브 Supabase에 insert→load→assert→**반드시 cleanup delete**), ③ 브라우저 프리뷰(port 8511)로 한다.
- `lib/` 코드 변경은 Streamlit 재시작해야 반영됨: `taskkill //F //PID <8511 PID>` 후 `preview_start`.
- push 대상은 `main`. service_role 키는 클라이언트 노출 금지. `.streamlit/secrets.toml`은 gitignore 유지.
- UI 카피는 한국어. 위치는 **선택(옵션)** — 미지정 허용. 별지9 PDF 서식은 불변.
- 위치 = 선택 사항: 미선택 오동작은 통합 목록에서 `위치 미지정`으로 표기.

---

### Task 1: 마이그레이션 B — malfunctions 위치 컬럼 추가

**Files:**
- Create: `docs/superpowers/migrations/2026-07-15-malfunction-location.sql`
- Apply: Supabase MCP `apply_migration` (프로젝트 `amyirlqfpwhcdmzzytoi`)

**Interfaces:**
- Produces: `malfunctions.floor text`, `malfunctions.zone text`, `malfunctions.spot_id text` (모두 nullable). Task 2가 이 컬럼을 읽고 쓴다.

- [ ] **Step 1: 마이그레이션 SQL 파일 작성**

`docs/superpowers/migrations/2026-07-15-malfunction-location.sql`:
```sql
-- 별지9 오동작에 위치 정보(선택) 추가 — v1.9
ALTER TABLE malfunctions ADD COLUMN IF NOT EXISTS floor text;
ALTER TABLE malfunctions ADD COLUMN IF NOT EXISTS zone text;
ALTER TABLE malfunctions ADD COLUMN IF NOT EXISTS spot_id text;
```

- [ ] **Step 2: Supabase MCP로 적용**

`mcp__d63dc937-b134-4865-99b2-f9a8d2bbc8e6__apply_migration` 호출:
- `project_id`: `amyirlqfpwhcdmzzytoi`
- `name`: `malfunction_location_v19`
- `query`: 위 SQL

- [ ] **Step 3: 컬럼 생성 확인**

`mcp__d63dc937-b134-4865-99b2-f9a8d2bbc8e6__execute_sql` 로:
```sql
select column_name, data_type, is_nullable
from information_schema.columns
where table_name = 'malfunctions' and column_name in ('floor','zone','spot_id')
order by column_name;
```
Expected: `floor / zone / spot_id` 3행, 모두 `is_nullable = YES`.

- [ ] **Step 4: 메모리 기록 (재실행 안내 방지)**

`memory/pyrosafe-v18-migrations.md`에 한 줄 추가: `malfunctions floor/zone/spot_id 컬럼 v1.9 적용됨(재실행 안내 금지)`. 필요 시 `MEMORY.md` 포인터 갱신.

- [ ] **Step 5: 커밋**

```bash
git add "docs/superpowers/migrations/2026-07-15-malfunction-location.sql" memory/
git commit -m "chore(db): malfunctions 위치 컬럼(floor/zone/spot_id) 마이그레이션 v1.9"
```

---

### Task 2: Malfunction 모델 + 데이터 레이어 위치 필드

**Files:**
- Modify: `lib/data.py` — `Malfunction` dataclass(309-322), `_row_to_malfunction`(472-481), `add_malfunction`(1047-1057)
- Verify(헤드리스): `scratchpad/verify_mal_location.py` (임시, 커밋 안 함)

**Interfaces:**
- Consumes: Task 1의 malfunctions.floor/zone/spot_id 컬럼.
- Produces: `Malfunction`에 `floor: str = ""`, `zone: str = ""`, `spot_id: str | None = None`. Task 4(다이얼로그)와 Task 5(통합 목록)가 이 필드를 읽고 쓴다.

- [ ] **Step 1: 헤드리스 라운드트립 검증 스크립트 작성 (실패 확인용)**

`scratchpad/verify_mal_location.py`:
```python
from datetime import date
from lib import data
from lib.data import Malfunction, add_malfunction, _db

mid = "MAL-VERIFY-LOC"
add_malfunction(Malfunction(
    malfunction_id=mid, category="감지기", occurred_on=date(2026, 7, 15),
    detail="위치필드 검증", action="", confirmer="tester",
    floor="B2", zone="SEC4", spot_id="SPOT-B2-001",
))
data._malfunction_rows.clear()
got = next(m for m in data.load_malfunctions() if m.malfunction_id == mid)
assert got.floor == "B2", got.floor
assert got.zone == "SEC4", got.zone
assert got.spot_id == "SPOT-B2-001", got.spot_id
print("ROUNDTRIP OK")
# cleanup (필수)
_db().table("malfunctions").delete().eq("malfunction_id", mid).execute()
print("CLEANUP OK")
```

- [ ] **Step 2: 검증 실행 → 실패 확인**

Run: `python scratchpad/verify_mal_location.py`
Expected: `TypeError: __init__() got an unexpected keyword argument 'floor'` (모델에 필드 없음).

- [ ] **Step 3: Malfunction dataclass에 필드 추가**

`lib/data.py` `Malfunction`(322행 `action_note` 아래)에 추가:
```python
    action_note: str = ""
    floor: str = ""          # v1.9: 오동작 발생 위치(선택)
    zone: str = ""
    spot_id: str | None = None
```

- [ ] **Step 4: `_row_to_malfunction` 매핑 추가**

`lib/data.py` `_row_to_malfunction`(480행 `action_note=...` 아래)에 추가:
```python
        action_note=r.get("action_note") or "",
        floor=r.get("floor") or "",
        zone=r.get("zone") or "",
        spot_id=r.get("spot_id"),
    )
```

- [ ] **Step 5: `add_malfunction` insert dict 추가**

`lib/data.py` `add_malfunction`(1055행 `"action_note": m.action_note,` 아래)에 추가:
```python
        "action_note": m.action_note,
        "floor": m.floor,
        "zone": m.zone,
        "spot_id": m.spot_id,
    }).execute()
```

- [ ] **Step 6: 검증 실행 → 통과 확인**

Run: `python scratchpad/verify_mal_location.py`
Expected: `ROUNDTRIP OK` 와 `CLEANUP OK` 출력.

- [ ] **Step 7: 커밋**

```bash
git add lib/data.py
git commit -m "feat(data): Malfunction 위치 필드(floor/zone/spot_id) + 매핑/insert"
```

---

### Task 3: 범용 위치 도면 픽커 `_location_map_picker`

**Files:**
- Modify: `lib/inspection_dialog.py` — 신규 함수 추가(예: `_add_task_map_picker` 정의 위/아래, 599행 근처)

**Interfaces:**
- Consumes: `lib.floor_widget`의 `control_toggle(key, default_locked)`, `legend_html(list)`, `plotly_config()`, `lock_overlay_css()`; `data.load_equipment()`, `data.load_spots(floor?)`. Equipment는 `.floor/.zone/.spot_id/.category/.equipment_id/.equipment_name` 속성 보유.
- Produces: `_location_map_picker(key_prefix: str, highlight_category: str | None = None) -> dict | None`. 반환 dict = `{"floor": str, "zone": str, "spot_id": str | None, "label": str}`. 선택 전이면 `None`. Task 4가 호출한다.

- [ ] **Step 1: 함수 구현 추가**

`lib/inspection_dialog.py`에 아래 함수를 추가한다(모듈 상단에 `from lib import data`, `import streamlit as st`는 이미 존재):
```python
def _location_map_picker(key_prefix: str, highlight_category: str | None = None):
    """회차 컨텍스트 없는 범용 위치 픽커. 도면에서 장비/빈 spot 1건 선택.
    반환: {"floor","zone","spot_id","label"} 또는 None(미선택).
    highlight_category와 category가 일치하는 장비를 파란색 강조."""
    import base64
    from pathlib import Path
    import plotly.graph_objects as go
    from lib.floor_widget import (
        control_toggle, legend_html, plotly_config, lock_overlay_css,
    )

    ASSETS_DIR = Path(__file__).resolve().parent.parent / "assets" / "floors"
    FIG_W, FIG_H = 2978, 2105

    all_eq = data.load_equipment()
    all_spots = data.load_spots()
    floors = sorted({e.floor for e in all_eq if e.floor} | {s.floor for s in all_spots})
    if not floors:
        st.info("도면을 표시할 수 있는 층이 없습니다.")
        return st.session_state.get(f"{key_prefix}_picked")

    floor = st.selectbox("층", options=floors, key=f"{key_prefix}_floor")

    cc, lc = st.columns([1.2, 5])
    with cc:
        locked = control_toggle(f"{key_prefix}_lock", default_locked=True)
    with lc:
        st.markdown(legend_html([
            ("#2563EB", "분류 일치 장비"),
            ("#94A3B8", "그 외 장비 · ◇ 빈 위치"),
        ]), unsafe_allow_html=True)

    img_path = ASSETS_DIR / f"{floor}.png"
    if not img_path.exists():
        st.warning(f"{floor} 도면 이미지가 없습니다.")
        return st.session_state.get(f"{key_prefix}_picked")
    uri = "data:image/png;base64," + base64.b64encode(img_path.read_bytes()).decode()

    fig = go.Figure()
    fig.add_layout_image(dict(
        source=uri, xref="x", yref="y",
        x=0, y=FIG_H, sizex=FIG_W, sizey=FIG_H,
        sizing="stretch", layer="below", opacity=1.0,
    ))

    spots = {s.spot_id: s for s in data.load_spots(floor)}
    floor_eq = [e for e in all_eq if e.floor == floor and e.spot_id in spots]
    match_xs, match_ys, match_txt, match_cd = [], [], [], []
    nomat_xs, nomat_ys, nomat_txt, nomat_cd = [], [], [], []
    for e in floor_eq:
        sp = spots.get(e.spot_id)
        if not sp:
            continue
        x = sp.x_pct / 100 * FIG_W
        y = FIG_H - sp.y_pct / 100 * FIG_H
        txt = e.equipment_id.split("-")[-1]
        cd = ("eq", e.spot_id, e.zone, e.equipment_name, e.equipment_id)
        if highlight_category and e.category == highlight_category:
            match_xs.append(x); match_ys.append(y)
            match_txt.append(txt); match_cd.append(cd)
        else:
            nomat_xs.append(x); nomat_ys.append(y)
            nomat_txt.append(txt); nomat_cd.append(cd)
    if nomat_xs:
        fig.add_trace(go.Scatter(
            x=nomat_xs, y=nomat_ys, mode="markers+text",
            text=nomat_txt, textposition="top center",
            textfont=dict(size=10, color="#475569"),
            marker=dict(size=16, color="#94A3B8",
                        line=dict(color="#FFFFFF", width=1.5)),
            customdata=nomat_cd,
            hovertemplate=("<b>%{customdata[3]}</b><br>"
                           "%{customdata[2]} · %{customdata[4]}<extra></extra>"),
            showlegend=False,
        ))
    if match_xs:
        fig.add_trace(go.Scatter(
            x=match_xs, y=match_ys, mode="markers+text",
            text=match_txt, textposition="top center",
            textfont=dict(size=10, color="#0F172A"),
            marker=dict(size=18, color="#2563EB",
                        line=dict(color="#FFFFFF", width=2)),
            customdata=match_cd,
            hovertemplate=("<b>%{customdata[3]}</b> (분류 일치)<br>"
                           "%{customdata[2]} · %{customdata[4]}<extra></extra>"),
            showlegend=False,
        ))

    used = {e.spot_id for e in all_eq if e.spot_id and e.floor == floor}
    empty = [s for s in spots.values() if s.spot_id not in used]
    if empty:
        xs, ys, cd = [], [], []
        for sp in empty:
            xs.append(sp.x_pct / 100 * FIG_W)
            ys.append(FIG_H - sp.y_pct / 100 * FIG_H)
            cd.append(("spot", sp.spot_id, sp.room_name, sp.room_name, sp.spot_id))
        fig.add_trace(go.Scatter(
            x=xs, y=ys, mode="markers",
            marker=dict(size=14, color="#94A3B8",
                        line=dict(color="#FFFFFF", width=1.5), symbol="diamond"),
            customdata=cd,
            hovertemplate=("<b>%{customdata[2]}</b> (빈 위치)<br>"
                           "%{customdata[1]}<extra></extra>"),
            showlegend=False,
        ))

    fig.update_xaxes(visible=False, range=[0, FIG_W], constrain="domain")
    fig.update_yaxes(visible=False, range=[0, FIG_H], scaleanchor="x", scaleratio=1)
    fig.update_layout(
        margin=dict(l=0, r=0, t=0, b=0),
        plot_bgcolor="#F8FAFC", height=480,
        dragmode="pan", showlegend=False,
        clickmode="event+select",
        uirevision=f"{key_prefix}_{floor}",
    )
    if locked:
        lock_overlay_css()
    event = st.plotly_chart(
        fig, use_container_width=True, config=plotly_config(),
        on_select="rerun", selection_mode=["points"],
        key=f"{key_prefix}_chart_{floor}",
    )

    picked_key = f"{key_prefix}_picked"
    if (not locked and event and getattr(event, "selection", None)
            and getattr(event.selection, "points", None)):
        pt = event.selection.points[-1]
        cd = pt.get("customdata")
        if cd:
            _kind, spot_id, zone, name, _ident = cd
            st.session_state[picked_key] = {
                "floor": floor, "zone": zone, "spot_id": spot_id,
                "label": f"{floor} / {zone} · {name}",
            }
    return st.session_state.get(picked_key)
```

- [ ] **Step 2: 구문 검증**

Run: `python -c "import ast; ast.parse(open('lib/inspection_dialog.py', encoding='utf-8').read()); print('AST OK')"`
Expected: `AST OK`

- [ ] **Step 3: import 검증**

Run: `python -c "import lib.inspection_dialog as m; assert hasattr(m, '_location_map_picker'); print('IMPORT OK')"`
Expected: `IMPORT OK` (라이브 Supabase 연결됨)

- [ ] **Step 4: 커밋**

```bash
git add lib/inspection_dialog.py
git commit -m "feat(ui): 범용 위치 도면 픽커 _location_map_picker (분류 일치 강조)"
```

---

### Task 4: 오동작 다이얼로그에 위치 픽커 장착

**Files:**
- Modify: `lib/inspection_dialog.py` — `malfunction_dialog`(1466-1516)

**Interfaces:**
- Consumes: `_location_map_picker(key_prefix, highlight_category)`(Task 3), `Malfunction(..., floor, zone, spot_id)`(Task 2).
- Produces: 없음(단말 UI). 오동작 등록 시 위치가 malfunctions에 저장됨.

- [ ] **Step 1: 오동작 내용 아래에 위치(선택) 영역 추가**

`lib/inspection_dialog.py` `malfunction_dialog`에서 `detail = st.text_area(...)`(1494-1498) **다음**, `if st.button("등록", ...)`(1500) **앞**에 삽입:
```python
    st.markdown(
        "<div style='color:#64748B; font-size:0.84rem; margin:0.4rem 0 0.2rem;'>"
        "<b>위치 (선택)</b> — 오동작 발생 위치를 도면에서 선택합니다. "
        "도면 밖·불명 위치는 비워둡니다.</div>",
        unsafe_allow_html=True,
    )
    picked_loc = _location_map_picker("mal_dlg_loc", highlight_category=category)
    if picked_loc:
        pc1, pc2 = st.columns([4, 1])
        with pc1:
            st.success(f"선택 위치: {picked_loc['label']}")
        with pc2:
            if st.button("위치 지우기", key="mal_dlg_loc_clear",
                         use_container_width=True):
                st.session_state.pop("mal_dlg_loc_picked", None)
                st.rerun()
    else:
        st.caption("위치 미지정 — 필요 시 위 도면에서 선택하세요.")
```

- [ ] **Step 2: 등록 제출 시 위치 저장**

`lib/inspection_dialog.py` `malfunction_dialog`의 `add_malfunction(Malfunction(...))`(1506-1514) 호출을 위치 포함으로 교체:
```python
        _loc = st.session_state.get("mal_dlg_loc_picked") or {}
        new_id = data.next_malfunction_id()
        add_malfunction(Malfunction(
            malfunction_id=new_id,
            category=category,  # type: ignore[arg-type]
            occurred_on=occurred,
            detail=detail.strip(),
            action="",
            confirmer=reporter,
            action_done=False,
            floor=_loc.get("floor", ""),
            zone=_loc.get("zone", ""),
            spot_id=_loc.get("spot_id"),
        ))
        st.session_state.pop("mal_dlg_loc_picked", None)
        st.session_state["just_submitted_malfunction"] = True
        st.rerun()
```

- [ ] **Step 3: 구문 검증**

Run: `python -c "import ast; ast.parse(open('lib/inspection_dialog.py', encoding='utf-8').read()); print('AST OK')"`
Expected: `AST OK`

- [ ] **Step 4: 프리뷰 확인**

Streamlit 재시작 후(`taskkill //F //PID <8511 PID>` → `preview_start`), 안전점검 관리 우상단 **[오동작 등록]** → 다이얼로그에서 소방시설 구분 선택 → 도면 픽커 표시 → 잠금 토글 OFF 후 마커 클릭 → "선택 위치: …" 배너 확인. (프리뷰가 모달을 못 열면 AST/헤드리스로 대체하고 사용자에게 Ctrl+F5 확인 요청)

- [ ] **Step 5: 커밋**

```bash
git add lib/inspection_dialog.py
git commit -m "feat(ui): 오동작 등록에 도면 위치 픽커(선택) 장착 + 저장"
```

---

### Task 5: 통합 목록 오동작 행 재배치

**Files:**
- Modify: `pages_app/deficiency_manager.py` — `_build_unified_rows` 오동작 매핑(74-89)
- Verify(헤드리스): `scratchpad/verify_unified_row.py` (임시, 커밋 안 함)

**Interfaces:**
- Consumes: `Malfunction.floor/zone/category`(Task 2).
- Produces: 없음(표시 로직). 오동작 UnifiedRow의 `location=층/구역`, `category=소방시설 구분`.

- [ ] **Step 1: 헤드리스 검증 스크립트 작성 (실패 확인용)**

`scratchpad/verify_unified_row.py`:
```python
from datetime import date
from lib import data
from lib.data import Malfunction, add_malfunction, _db
from pages_app.deficiency_manager import _build_unified_rows

mid = "MAL-VERIFY-ROW"
add_malfunction(Malfunction(
    malfunction_id=mid, category="감지기", occurred_on=date(2026, 7, 15),
    detail="행배치 검증", action="", confirmer="tester",
    floor="B2", zone="SEC4", spot_id="SPOT-B2-001",
))
data._malfunction_rows.clear()
row = next(r for r in _build_unified_rows() if r.raw_id == mid)
assert row.location == "B2 / SEC4", row.location
assert row.category == "감지기", row.category
print("ROW OK")
_db().table("malfunctions").delete().eq("malfunction_id", mid).execute()
print("CLEANUP OK")
```

- [ ] **Step 2: 검증 실행 → 실패 확인**

Run: `python scratchpad/verify_unified_row.py`
Expected: `AssertionError` — `row.location`이 `감지기`(현재 매핑), `B2 / SEC4` 아님.

- [ ] **Step 3: 오동작 UnifiedRow 매핑 변경**

`pages_app/deficiency_manager.py` 오동작 append(77-89)의 `location`/`category` 두 줄 교체:
```python
        rows.append(UnifiedRow(
            type="오동작",
            date=m.occurred_on,
            location=(f"{m.floor} / {m.zone}"
                      if (m.floor or m.zone) else "위치 미지정"),
            category=m.category,
            content=m.detail,
            status=status,
            notice_no="-",
            raw_id=m.malfunction_id,
            action_done=m.action_done,
            task_id=m.task_id or "-",
            round_id=(task_round_map.get(m.task_id, "") or "-") if m.task_id else "-",
        ))
```

- [ ] **Step 4: 검증 실행 → 통과 확인**

Run: `python scratchpad/verify_unified_row.py`
Expected: `ROW OK` 와 `CLEANUP OK`.

- [ ] **Step 5: 구문 검증**

Run: `python -c "import ast; ast.parse(open('pages_app/deficiency_manager.py', encoding='utf-8').read()); print('AST OK')"`
Expected: `AST OK`

- [ ] **Step 6: 프리뷰 확인 (선택)**

재시작 후 작업 조치 관리 → 오동작 행이 `장소시설=층/구역`, `점검종류=소방시설 구분`으로 지적사항 행과 동일 열 정렬인지 확인. 위치 미지정 오동작은 `위치 미지정` 표기.

- [ ] **Step 7: 커밋 + 푸시**

```bash
git add pages_app/deficiency_manager.py
git commit -m "feat(tasks): 통합 목록 오동작 행 재배치 — location=층/구역, category=소방시설구분"
git push origin main
```

---

## Self-Review (완료)

**스펙 커버리지:** 마이그레이션 B → Task 1 ✅ · 모델/데이터 → Task 2 ✅ · 도면 픽커 신설 → Task 3 ✅ · 다이얼로그 장착(분류 일치 강조·선택 옵션) → Task 4 ✅ · 통합 목록 행 재배치 → Task 5 ✅ · 별지9 PDF 불변 → 변경 없음(범위 밖) ✅ · 관리대장 컬럼(B-5) → "여력 시 선택"이므로 미포함(사용자 확정).

**플레이스홀더 스캔:** 없음. 모든 코드 스텝에 실제 코드 포함.

**타입 일관성:** `_location_map_picker` 반환 dict 키(`floor/zone/spot_id/label`)를 Task 4에서 동일 키로 소비. `Malfunction` 필드명(`floor/zone/spot_id`)이 Task 2 정의 → Task 4 생성 → Task 5 소비에서 일치. session key `mal_dlg_loc_picked`는 픽커의 `{key_prefix}_picked` = `mal_dlg_loc_picked`로 일치.
