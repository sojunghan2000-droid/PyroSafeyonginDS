# 도움말(FAQ) 개편 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 상단바 "?" 도움말을 카테고리(공통+6화면)로 재배치하고, 내용을 v1.8~1.10까지 갱신하며, 경량 오프라인 유사도 검색을 추가한다.

**Architecture:** `lib/ui.py` 단일 파일. `_HELP_FAQ`(평면 20개)를 `_HELP_FAQ_BY_CAT`(카테고리 순서 구조) + `_HELP_FAQ_FLAT`(검색용 파생)로 교체하고, 표준 라이브러리만 쓰는 `_faq_search`를 추가하고, `_help_dialog`에 검색창+카테고리 섹션 렌더를 넣는다. 외부 의존·스키마 변경 없음.

**Tech Stack:** Streamlit 1.51, Python 3.11 표준 라이브러리(`difflib`, `re`).

## Global Constraints

- 이 저장소엔 pytest 스위트가 없다. 검증은 ① `python -c "import ast; ast.parse(...)"` 구문, ② 헤드리스 `python -c "from lib.ui import ...; ..."`(라이브 Supabase 연결되나 이 기능은 순수 함수/상수라 DB 무관) assert, ③ 브라우저 프리뷰(port 8511).
- 외부 라이브러리 추가 금지 — `difflib`, `re`만 사용(둘 다 표준 라이브러리).
- FAQ는 코드 내 상수, 스키마/DB 변경 없음.
- 기존 20개 FAQ **답변 텍스트는 원문 유지**(재분류로 이동만), 단 명시된 stale 문구만 정정.
- `lib/` 코드 변경은 Streamlit 재시작해야 반영됨.
- push 대상은 `main`. UI 카피는 한국어.

---

### Task 1: 경량 유사도 검색 함수 `_faq_search`

**Files:**
- Modify: `lib/ui.py` — `_HELP_FAQ` 정의 부근(예: [ui.py:559](../../../lib/ui.py) 위 또는 아래)에 함수 추가. 파일 상단에 `import difflib`, `import re` 없으면 추가.
- Verify(헤드리스): `scratchpad/verify_faq_search.py` (임시, 커밋 안 함)

**Interfaces:**
- Produces: `_faq_search(query: str, items: list[tuple[str,str,str]]) -> list[tuple[float,str,str,str]]`. items=(cat,q,a), 반환=(score,cat,q,a) 내림차순. Task 3이 소비.

- [ ] **Step 1: 헤드리스 검증 스크립트 작성 (실패 확인용)**

`scratchpad/verify_faq_search.py`:
```python
from lib.ui import _faq_search
sample = [
    ("작업 조치 관리", "오동작은 어떻게 등록·조치하나요?", "점검 중 발견 또는 오동작 등록 버튼"),
    ("보고서", "PDF는 어디서 출력하나요?", "별지5/6/9 전체 PDF"),
    ("시설 관리", "QR 코드는 어디서 발급하나요?", "시설 관리 각 장비 행 속성 버튼"),
]
res = _faq_search("오동작", sample)
assert res and res[0][1] == "작업 조치 관리", res
res2 = _faq_search("별지5", sample)
assert res2 and "PDF" in res2[0][2], res2
assert _faq_search("", sample) == []
print("SEARCH OK")
```

- [ ] **Step 2: 실행 → 실패 확인**

Run: `python scratchpad/verify_faq_search.py`
Expected: `ImportError: cannot import name '_faq_search'`

- [ ] **Step 3: 함수 추가**

`lib/ui.py`에 추가(상단 import에 `import difflib`, `import re` 보장):
```python
def _faq_search(query: str, items: list[tuple[str, str, str]]
                ) -> list[tuple[float, str, str, str]]:
    """query와 각 (cat, q, a)의 유사도 점수를 계산해 임계 이상만 내림차순 반환."""
    q = query.strip().lower()
    if not q:
        return []
    q_tokens = set(re.findall(r"[0-9a-z가-힣]+", q))
    scored: list[tuple[float, str, str, str]] = []
    for cat, question, answer in items:
        ql, hay = question.lower(), f"{question} {answer}".lower()
        sub = 1.0 if q in ql else (0.6 if q in hay else 0.0)
        h_tokens = set(re.findall(r"[0-9a-z가-힣]+", hay))
        overlap = (len(q_tokens & h_tokens) / len(q_tokens)) if q_tokens else 0.0
        seq = difflib.SequenceMatcher(None, q, ql).ratio()
        score = max(sub, overlap, 0.5 * overlap + 0.5 * seq)
        if score >= 0.3:
            scored.append((score, cat, question, answer))
    scored.sort(key=lambda x: x[0], reverse=True)
    return scored
```

- [ ] **Step 4: 실행 → 통과 확인**

Run: `python scratchpad/verify_faq_search.py`
Expected: `SEARCH OK`

- [ ] **Step 5: 구문 확인**

Run: `python -c "import ast; ast.parse(open('lib/ui.py', encoding='utf-8').read()); print('AST OK')"`
Expected: `AST OK`

- [ ] **Step 6: 커밋**

```bash
git add lib/ui.py
git commit -m "feat(ui): 도움말 경량 유사도 검색 함수 _faq_search"
```

---

### Task 2: FAQ 카테고리 데이터 구조 + 내용 갱신

**Files:**
- Modify: `lib/ui.py` — `_HELP_FAQ`([559-685](../../../lib/ui.py))를 `_HELP_FAQ_BY_CAT` + `_HELP_FAQ_FLAT`로 교체
- Verify(헤드리스): `scratchpad/verify_faq_data.py` (임시, 커밋 안 함)

**Interfaces:**
- Produces: `_HELP_FAQ_BY_CAT: list[tuple[str, list[tuple[str,str]]]]`, `_HELP_FAQ_FLAT: list[tuple[str,str,str]]`. Task 3이 소비.

**기존 20개 → 카테고리 매핑** (질문 제목 기준. **답변 원문은 그대로 이동**):
- **공통**: `알림 벨의 숫자는 무엇인가요?` / `QR 스캔했더니 '어느 회차 점검인가요?'가 떠요` / `음성 입력(STT)은 어떻게 사용하나요?` / `데이터가 자동 저장되나요?`
- **시설 관리**: `QR 코드는 어디서 발급하나요?` / `QR 발급·부착 절차는 어떻게 되나요?` / `위치(spot)는 어떻게 정의하나요?` / `장비 ID · spot ID · 위치는 어떻게 다른가요?`
- **안전점검 관리**: `점검은 어떻게 시작하나요?` / `점검 결과는 어떻게 선택하나요?` / `화기작업구간 점검은 어떻게 진행하나요?` / `가설컨테이너 사무실 점검은?` / `화기작업/가설컨테이너 점검의 세부 checklist가 뭔가요?` / `양호인데 세부 항목이 다 필요한가요?` / `불량 사유 카탈로그는 어디서 관리되나요?` / `도면에 없는 위치는 어떻게 등록하나요?` / `점검 회차(Round)는 무엇인가요?`
- **작업 조치 관리**: `오동작은 어떻게 등록·조치하나요?` / `불량 점검 결과의 후속 조치는 어떻게 하나요?`
- **보고서**: `PDF는 어디서 출력하나요?`
- **관리자 메뉴**: (기존 항목 없음 — 신규 1건은 아래에서 추가하지 않음. 빈 카테고리는 구조엔 넣되 렌더 시 생략되므로, 관리자 메뉴 카테고리는 **생략**한다.)

**stale 문구 정정** (답변 이동 시 함께):
- `점검은 어떻게 시작하나요?` — "탭" 표현이 있으면 "라디오"로. 신규 위치 점검 진입 문구 현행화.
- `도면에 없는 위치는 어떻게 등록하나요?` / `위치(spot)는 어떻게 정의하나요?` — "신규 위치 점검 탭" → "신규 위치 점검"(라디오) 정도로 정정(의미 유지, 과도한 재작성 금지).

**신규 항목 추가** (아래 답변 **그대로** 사용):

안전점검 관리에 추가:
```python
("점검 진입 방식이 바뀌었나요? (v1.8)",
 "[+ Task 추가]와 장소·구역 정정의 진입이 **탭에서 라디오 버튼으로** 바뀌었습니다.\n"
 "(1) **장비 선택 (사전 위치 설정됨)** — 등록 장비를 목록·QR로 선택\n"
 "(2) **신규 위치 점검** — 도면에서 위치를 직접 지정\n"
 "라디오는 세션에 저장되어, 도면 잠금 토글이나 지도 조작으로 화면이 갱신돼도 "
 "**선택한 방식이 유지**됩니다(초기화 안 됨)."),
("장비 위치가 틀렸어요 — 정정하려면? (v1.9)",
 "점검 시작 인라인 입력에서 **[장소·구역 정정 (선택)]** 영역을 사용합니다.\n"
 "(1) **QR 스캔** — 다른 장비 QR로 위치 재인식\n"
 "(2) **도면 spot 선택** — 도면에서 올바른 위치 마커 클릭\n"
 "정정 전에도 **장비의 현재 위치가 파란 마커**로 표시되고, 다른 위치는 옅은 주황으로 "
 "나타납니다. 정정한 값은 별지5 row에 반영됩니다."),
```

작업 조치 관리에 추가:
```python
("직접 등록한 오동작에 점검 ID·작업 ID가 왜 생기나요? (v1.10)",
 "[오동작 등록]으로 **직접 등록**하면, 추적을 위해 **'오동작 접수' 유형의 점검 회차와 "
 "작업(Task)이 자동 발행**되어 오동작에 연결됩니다.\n"
 "(1) 그래서 작업 조치 관리 목록에 점검 ID(INS-…)·작업 ID(TSK-…)가 실제 값으로 표시됩니다.\n"
 "(2) 안전점검 관리 회차 목록엔 **'오동작 접수' 빨강 배지**로 정기 점검과 구분되어 나타납니다.\n"
 "(3) 정기 점검이 아니므로 **점검 KPI·별지5 회차 목록에는 포함되지 않습니다.**"),
("오동작 발생 위치는 어떻게 기록하나요? (v1.9)",
 "[오동작 등록] 다이얼로그의 **위치 (선택)** 영역에서 도면의 위치(spot)나 장비를 클릭해 "
 "발생 위치를 지정합니다(선택 사항 — 도면 밖·불명 시 비움).\n"
 "선택한 분류(소방시설 구분)와 일치하는 장비는 파란색으로 강조됩니다.\n"
 "작업 조치 관리 통합 목록에서 오동작 행은 지적사항 행과 **동일한 열 구조**"
 "(장소=층/구역, 점검종류=소방시설 구분)로 정렬됩니다."),
```

공통에 추가:
```python
("용어 — 별지5·6·9가 뭔가요?",
 "소방 안전점검 법정 서식입니다.\n"
 "(1) **별지5** — 안전점검 결과 지적 내역서 (점검 결과·지적사항)\n"
 "(2) **별지6** — (지적사항) 통보서 (불량 발생 시 발급·조치)\n"
 "(3) **별지9** — 소방시설 오동작 관리대장 (운영 중 오동작 기록)\n"
 "각 PDF는 📄 보고서에서 출력합니다."),
```

- [ ] **Step 1: `_HELP_FAQ` → `_HELP_FAQ_BY_CAT` + `_HELP_FAQ_FLAT` 교체**

`lib/ui.py`의 `_HELP_FAQ = [ ... ]`(559-685) 전체를 아래 구조로 교체. 각 카테고리 리스트에 위 매핑대로 기존 (질문, 답변) 튜플을 **원문 답변 그대로** 배치하고, 신규 항목을 해당 카테고리에 추가한다. stale 문구만 정정.
```python
_HELP_FAQ_BY_CAT: list[tuple[str, list[tuple[str, str]]]] = [
    ("공통", [
        # 알림 벨 / QR 어느 회차 / 음성 입력 / 데이터 자동 저장 / 용어 별지5·6·9(신규)
    ]),
    ("시설 관리", [
        # QR 발급 / QR 발급·부착 절차 / 위치(spot) 정의 / 장비·spot ID 차이
    ]),
    ("안전점검 관리", [
        # 점검 시작 / 점검 결과 / 화기작업 / 가설컨테이너 / 세부 checklist /
        # 양호 세부 / 불량 사유 / 도면 없는 위치 / 점검 회차란 /
        # 진입 방식(신규) / 위치 정정(신규)
    ]),
    ("작업 조치 관리", [
        # 오동작 등록·조치 / 불량 후속 조치 / 오동작 접수 회차(신규) / 오동작 위치(신규)
    ]),
    ("보고서", [
        # PDF 출력
    ]),
]
_HELP_FAQ_FLAT: list[tuple[str, str, str]] = [
    (cat, q, a) for cat, items in _HELP_FAQ_BY_CAT for (q, a) in items
]
```

- [ ] **Step 2: 헤드리스 검증 스크립트 작성**

`scratchpad/verify_faq_data.py`:
```python
from lib.ui import _HELP_FAQ_BY_CAT, _HELP_FAQ_FLAT
cats = [c for c, _ in _HELP_FAQ_BY_CAT]
assert cats == ["공통", "시설 관리", "안전점검 관리", "작업 조치 관리", "보고서"], cats
titles = [q for _, q, _ in _HELP_FAQ_FLAT]
# 기존 20 + 신규 5 = 25
assert len(_HELP_FAQ_FLAT) == 25, len(_HELP_FAQ_FLAT)
assert any("오동작 접수" in q or "점검 ID·작업 ID" in q for q in titles)
assert any("별지5·6·9" in q for q in titles)
assert any("진입 방식" in q for q in titles)
print("FAQ-DATA OK", len(_HELP_FAQ_FLAT))
```

- [ ] **Step 3: 실행 → 통과 확인**

Run: `python scratchpad/verify_faq_data.py`
Expected: `FAQ-DATA OK 25` (개수가 다르면 매핑 누락/중복 점검 후 조정)

- [ ] **Step 4: 구문 확인**

Run: `python -c "import ast; ast.parse(open('lib/ui.py', encoding='utf-8').read()); print('AST OK')"`
Expected: `AST OK`

- [ ] **Step 5: 커밋**

```bash
git add lib/ui.py
git commit -m "feat(ui): 도움말 FAQ 카테고리화(_HELP_FAQ_BY_CAT) + v1.8~1.10 항목 추가"
```

---

### Task 3: `_help_dialog` 렌더 개편 (검색창 + 카테고리 섹션)

**Files:**
- Modify: `lib/ui.py` — `_help_dialog`([688-698](../../../lib/ui.py))

**Interfaces:**
- Consumes: `_faq_search`(Task 1), `_HELP_FAQ_BY_CAT`/`_HELP_FAQ_FLAT`(Task 2).

- [ ] **Step 1: `_help_dialog` 본문 교체**

`lib/ui.py` `_help_dialog` 함수 본문을 교체:
```python
@st.dialog("도움말 — PyroSafe 사용 가이드", width="large")
def _help_dialog() -> None:
    query = st.text_input("검색", key="help_search",
                          placeholder="예: 오동작, QR, 별지5, 위치 정정")
    if query.strip():
        results = _faq_search(query, _HELP_FAQ_FLAT)
        st.caption(f"'{query}' 검색 결과 {len(results)}건")
        if not results:
            st.info("일치하는 항목이 없습니다. 다른 키워드로 검색해 보세요.")
        for _score, cat, q, a in results:
            with st.expander(f"[{cat}] {q}"):
                st.markdown(a)
    else:
        st.caption(
            f"자주 묻는 질문 {len(_HELP_FAQ_FLAT)}개 · 카테고리별. "
            "상단 검색으로 바로 찾을 수 있습니다."
        )
        for cat, items in _HELP_FAQ_BY_CAT:
            if not items:
                continue
            st.markdown(f"**{cat}**")
            for q, a in items:
                with st.expander(q):
                    st.markdown(a)
```

- [ ] **Step 2: 구문 확인**

Run: `python -c "import ast; ast.parse(open('lib/ui.py', encoding='utf-8').read()); print('AST OK')"`
Expected: `AST OK`

- [ ] **Step 3: import 확인**

Run: `python -c "import lib.ui; print('IMPORT OK')"`
Expected: `IMPORT OK`

- [ ] **Step 4: 프리뷰 확인 (컨트롤러)**

재시작 후 "?" → 카테고리 섹션 렌더 확인, 검색어("오동작"/"별지5") 입력 시 관련 항목 상위 노출, 없는 키워드로 0건 안내 확인. (프리뷰 불안정 시 AST/헤드리스로 대체하고 사용자 Ctrl+F5 확인 요청.)

- [ ] **Step 5: 커밋 + 푸시**

```bash
git add lib/ui.py
git commit -m "feat(ui): 도움말 다이얼로그 검색창 + 카테고리 섹션 렌더"
git push origin main
```

---

## Self-Review (완료)

**스펙 커버리지:** 검색 함수 → Task 1 ✅ · 카테고리 데이터+내용 갱신+신규 항목 → Task 2 ✅ · 렌더(검색창+섹션+동적 카운트) → Task 3 ✅ · 임베딩/DB 이관 등 범위 밖 → 미포함 ✅.

**플레이스홀더 스캔:** Task 2 Step 1의 카테고리 리스트 주석(`# ...`)은 매핑·신규항목 섹션에 **완전한 내용이 명시**돼 있어 실질 플레이스홀더 아님(기존 답변은 파일 원문 이동, 신규 5항목은 전문 제공). 그 외 없음.

**타입 일관성:** `_faq_search` 반환 `(score,cat,q,a)`를 Task 3에서 `for _score, cat, q, a in results`로 동일 언팩. `_HELP_FAQ_BY_CAT`(cat,[(q,a)]) / `_HELP_FAQ_FLAT`(cat,q,a) 형태가 Task 2 정의 → Task 3 소비에서 일치. 검증 스크립트의 개수(25)·카테고리 순서가 Task 2 구조와 일치.
